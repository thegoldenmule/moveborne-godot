

export const description = 'Trackables'

export const meta = {
  author: 'AJ Apte',
  editor: 'Alex Vlahopoulos',
}

# Trackables

The Snapser Trackables service enables your game or app to track a user's progress in your
game or other digital experience. This service supports adding various levels of progression based on a
numeric score.

## Concept

Plenty of games and social apps develop a structure for their level progression. This XP or energy
ends up being stored in a service like **storage**. While you can certainly go down that
route, any changes to the progression that your design team wants to control becomes
a nightmare to manage. The trackables service makes all this a breeze, especially for game or app dev teams.

## XP

XP is a numeric score that represents a player's progression in the game. 
The player progression can be represented in the form of levels. XP can be a single level or multiple levels.
{/* Incrementing the XP

The progression can be represented one dimensionally as a single score, or in the form of levels that the player can achieve.
The players XP can be incremented or decremented based on the player's
actions in the game.  */}

### Levels

Levels are nothing but numeric ranges that start and end at a
certain numeric score. Each XP can have one or more levels.

A final cap level can be created to represent the maximum level a player can achieve.

<Note>Overlapping levels and gaps in ranges are not supported.</Note>

### Auto Reset

When enabled, once all levels have been completed, the user's XP will be reset to 0. In case this setting is enabled then there cannot be a cap level

### Tracking

The game client needs to increment the XP score of the player based on the
player's actions in the game. The trackables service will automatically assign the level
to a user based on their XP score.

### Requirement
You can specify an optional requirement for starting a XP ladder. This allows you to chain your XP ladders,
essentially making one to be a requirement to complete before you can start a particular ladder.

<Note>
  Specifying a XP requirement is optional.
</Note>

### Scopes

Each XP supports the following scopes:

1. **External**: Progression visible from the client.
2. **Internal**: Progression visible only to Snaps within your cluster.

### Rewards
There are 2 types of rewards that can be setup for each level in a XP ladder:
1. **Level Completion Rewards**: These rewards are awarded to the user when they reach the end value of the current level.
2. **Level Up Rewards**: These rewards are awarded to the user when they reach the start value of the next level.

Each reward entry can be an Inventory Item, a Currency, a Drop Table, an Energy, or another XP. When granting an XP reward, you can pick any XP configured under Trackables, including the same XP that is being configured — this is useful for compounding progression or for boosting the current ladder from one of its own level thresholds.
<Note>
  All rewards are optional and can be omitted.
</Note>

## Energy
Energy is a resource that is used to play a game. It is a finite resource that can be replenished over time.
This mechanic can be used to represent a player's stamina or lives in a game.

### Scopes

Each Energy supports the following scopes:

1. **External**: Visible from the client.
2. **Internal**: Visible only to Snaps within your cluster.

### Parameters
Each energy has the following parameters:
1. **Maximum energy**: Maximum energy that a user can have
2. **Time to recover**: Time taken to recover 1 energy

### Overflow
Some games allow the energy to overflow beyond the maximum energy. This can be enabled or disabled based on the game design.
<Note>
  If energy overflow is activated, replenish will only kick in when the energy drops below the maximum energy.
</Note>

## Streaks
Streaks track consecutive activity windows in your game or app — daily logins, weekly check-ins, completing a session every hour, even monthly streaks, and so on. A streak is composed of a `period_duration` (interpreted in a configurable `time_unit`) and a list of `rewards` granted at specific streak positions.

### Type
Streaks come in two flavors. The type is chosen at creation time and **cannot be changed**:

1. **Fixed**: Periods are anchored to a global schedule. Windows start at `offset_seconds` from UTC midnight (or at UTC midnight when no offset is given) and roll over when the period duration ends. Cooldown does not apply.
2. **Rolling Window**: Periods are tracked per user. A user can increment their streak as long as they do so before `period_duration` elapses; after that, a `cooldown_duration_seconds` must pass before the next increment is allowed.

### Time Unit, Period, Offset & Cooldown
- `time_unit` — the unit for `period_duration`. One of `seconds`, `minutes`, `hours`, `weeks`, `months`. Rolling Window streaks are locked to `seconds`.
- `period_duration` — the length of one window expressed in `time_unit`. Applies to both types.
- `offset_seconds` — *(fixed only)* shifts the global window relative to UTC midnight. **Always in seconds**, regardless of `time_unit`.
- `cooldown_duration_seconds` — *(rolling_window only)* the wait between increments. **Always in seconds**, regardless of `time_unit`.

### Length
- `max_length` — the highest streak position a user can reach. Set to `0` for no maximum. If non-zero, all reward positions must be less than or equal to this value.

### Rewards
A streak has a list of `rewards`. The list can be empty — a streak with no rewards is a valid, tracking-only configuration. If you do add entries, each one has a unique `rung_index` (the streak position at which the reward is granted), a `name`, optional `metadata`, and a `level_completion_rewards` payload that can include Inventory Items, Currencies, Drop Tables, Energy, or XP. Unlike XP, **a Streak cannot reward another Streak**.
<Note>
  Reward positions must be unique. If `max_length` is set, positions must be less than or equal to it. Each reward entry must contain at least one reward item — an entry with no items is rejected on save.
</Note>

### Scopes
Each Streak supports the following scopes:

1. **External**: Progression visible from the client.
2. **Internal**: Progression visible only to Snaps within your cluster.

### Behavior
1. **Loop on Complete**: When enabled, the streak restarts from position 1 once the final reward is reached.
2. **Repeatable Rewards**: When enabled, rewards are re-granted on every loop instead of only the first time the streak is completed. This setting only has an effect when `loop_on_complete` is enabled.

## APIs & Emitters

The `Trackables` snap exposes the following APIs

<div className="apisTable">
| API Name            | HTTP Method | API Description                                                           |
| ------------------- | ----------- | ------------------------------------------------------------------------- |
| [TrackablesGetUserEnergy](#trackablesgetuserenergy-api)       | GET         | Gets the energy of the user                                               |
| [TrackablesConsumeUserEnergy](#trackablesconsumeuserenergy-api)   | PATCH       | Consumes the energy of the user                                           |
| [TrackablesIncrementUserEnergy](#trackablesincrementuserenergy-api) | PATCH       | Increments the energy of the user (App auth only)                         |
| [TrackablesGetUserXp](#trackablesgetuserxp-api)           | GET         | Gets the xp of given user, returns error if not set                       |
| [TrackablesIncrementUserXp](#trackablesincrementuserxp-api)     | PATCH       | Increments the xp of given user, starts from zero if a user xp is not set |
| [TrackablesSetUserXp](#trackablessetuserxp-api)           | PUT         | Sets the xp for a given user                                              |
| [TrackablesBatchConsumeUserEnergy](#trackablesbatchconsumeuserenergy-api)           | PATCH         | Consumes energy for users                                              |
| [TrackablesBatchGetUserEnergy](#trackablesbatchgetuserenergy-api)           | GET         | Gets the energy for users                                              |
| [TrackablesBatchGetUserTrackables](#trackablesbatchgetusertrackables-api)           | GET         | Gets xp and energy for users                                              |
| [TrackablesBatchGetUserXp](#trackablesbatchgetuserxp-api)           | GET         | Gets the xp for users                                              |
| [TrackablesBatchIncrementUserXp](#trackablesbatchincrementuserxp-api)           | PATCH         | Increments the xp for users                                              |
| [TrackablesBatchSetUserXp](#trackablesbatchsetuserxp-api)           | PATCH         | Sets the xp for users                                              |
| [TrackablesGetXp](#trackablesgetxp-api)             | GET         | Gets the setting of an XP |
| [TrackablesGetXps](#trackablesgetxps-api)           | GET         | Gets the settings of multiple XPs                                              |
| [TrackablesGetEnergy](#trackablesgetenergy-api)     | GET         | Gets the settings of multiple Energy Trackables |
</div>

### Usage
#### TrackablesGetUserEnergy API
<CodeGroup title="Gets the energy of the user" tag="GET" label="TrackablesGetUserEnergy">
```csharp
/// <summary>
/// User Energy (App Auth) Gets the energy of the users
/// </summary>
/// <exception cref="Snapser.Client.ApiException">Thrown when fails to make API call</exception>
/// <param name="energyName">Name of energy</param>
/// <param name="userId">User IDs to get energy for</param>
/// <param name="token">(app auth only)</param>
/// <returns>TrackablesBatchGetUserEnergyResponse</returns>
public TrackablesBatchGetUserEnergyResponse TrackablesBatchGetUserEnergy(string energyName, List<string> userId, string token)
{
    Snapser.Client.ApiResponse<TrackablesBatchGetUserEnergyResponse> localVarResponse = TrackablesBatchGetUserEnergyWithHttpInfo(energyName, userId, token);
    return localVarResponse.Data;
}

/// Snapser.Model for TrackablesBatchGetUserEnergyResponse
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesBatchGetUserEnergyResponse" /> class.
/// </summary>
/// <param name="responses">responses.</param>
public TrackablesBatchGetUserEnergyResponse(List<TrackablesGetUserEnergySummaryResponse> responses = default(List<TrackablesGetUserEnergySummaryResponse>))
{
    this.Responses = responses;
}

/// Snapser.Model for TrackablesGetUserEnergySummaryResponse
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesGetUserEnergySummaryResponse" /> class.
/// </summary>
/// <param name="energy">Energy of the user.</param>
/// <param name="energyName">Name of energy.</param>
/// <param name="metadata">Metadata associated with the energy setting.</param>
/// <param name="nextEnergyAt">Next energy at Timestamp.</param>
/// <param name="secondsToNextEnergy">Seconds to next energy.</param>
/// <param name="userId">UserID of the user.</param>
public TrackablesGetUserEnergySummaryResponse(int energy = default(int), string energyName = default(string), Object metadata = default(Object), long nextEnergyAt = default(long), int secondsToNextEnergy = default(int), string userId = default(string))
{
	this.Energy = energy;
	this.EnergyName = energyName;
	this.Metadata = metadata;
	this.NextEnergyAt = nextEnergyAt;
	this.SecondsToNextEnergy = secondsToNextEnergy;
	this.UserId = userId;
}

```
```cpp
// Request
class SNAPSER_API SnapserTrackablesServiceApi::TrackablesGetUserEnergyRequest : public Request
{
public:
    virtual ~TrackablesGetUserEnergyRequest() {}
	void SetupHttpRequest(const FHttpRequestRef& HttpRequest) const final;
	FString ComputePath() const final;

	/* UserID of user */
	FString UserId;
	/* Name of energy */
	FString EnergyName;
	/* User session token */
	FString Token;
};

// Response
class SNAPSER_API SnapserTrackablesServiceApi::TrackablesGetUserEnergyResponse : public Response
{
public:
    virtual ~TrackablesGetUserEnergyResponse() {}
	void SetHttpResponseCode(EHttpResponseCodes::Type InHttpResponseCode) final;
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;

    SnapserTrackablesUserEnergy Content;
};

class SNAPSER_API SnapserTrackablesUserEnergy : public Model
{
public:
    virtual ~SnapserTrackablesUserEnergy() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Energy of the user */
	TOptional<int32> Energy;
	/* Metadata associated with the energy setting */
	TOptional<TSharedPtr<FJsonObject>> Metadata;
	/* Next energy at Timestamp */
	TOptional<int64> NextEnergyAt;
	/* Seconds to next energy */
	TOptional<int32> SecondsToNextEnergy;
};

```
```go
// Coming soon
```
```python
# Coming soon
```
</CodeGroup>
#### TrackablesConsumeUserEnergy API
<CodeGroup title="Consumes the energy of the user" tag="PATCH" label="TrackablesConsumeUserEnergy">
```csharp
/// <summary>
/// Consume user energy Consumes the energy of the user
/// </summary>
/// <exception cref="Snapser.Client.ApiException">Thrown when fails to make API call</exception>
/// <param name="userId">UserID of the affected user</param>
/// <param name="energyName">Name of energy to consume</param>
/// <param name="token">User session token</param>
/// <param name="body"></param>
/// <returns>TrackablesUpdateUserEnergyInfo</returns>
public TrackablesUpdateUserEnergyInfo TrackablesConsumeUserEnergy(string userId, string energyName, string token, TrackablesServiceConsumeUserEnergyBody body)
{
    Snapser.Client.ApiResponse<TrackablesUpdateUserEnergyInfo> localVarResponse = TrackablesConsumeUserEnergyWithHttpInfo(userId, energyName, token, body);
    return localVarResponse.Data;
}

/// Snapser.Model for TrackablesServiceConsumeUserEnergyBody
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesServiceConsumeUserEnergyBody" /> class.
/// </summary>
/// <param name="delta">Value to decrement the energy by (required).</param>
public TrackablesServiceConsumeUserEnergyBody(long delta = default(long))
{
    this.Delta = delta;
}

/// Snapser.Model for TrackablesUpdateUserEnergyInfo
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesUpdateUserEnergyInfo" /> class.
/// </summary>
/// <param name="afterEnergy">Energy of user after the update.</param>
/// <param name="beforeEnergy">Energy of user before the update.</param>
/// <param name="energy">Energy of the user. (DEPRECATED: Use after_energy).</param>
/// <param name="metadata">Metadata associated with the energy setting.</param>
/// <param name="nextEnergyAt">Next energy at Timestamp.</param>
/// <param name="secondsToNextEnergy">Seconds to next energy.</param>
/// <param name="success">Status of energy update, true if energy updated successfully.</param>
/// <param name="userId">UserID of the user.</param>
public TrackablesUpdateUserEnergyInfo(int afterEnergy = default(int), int beforeEnergy = default(int), int energy = default(int), Object metadata = default(Object), long nextEnergyAt = default(long), int secondsToNextEnergy = default(int), bool success = default(bool), string userId = default(string))
{
	this.AfterEnergy = afterEnergy;
	this.BeforeEnergy = beforeEnergy;
	this.Energy = energy;
	this.Metadata = metadata;
	this.NextEnergyAt = nextEnergyAt;
	this.SecondsToNextEnergy = secondsToNextEnergy;
	this.Success = success;
	this.UserId = userId;
}
```
```cpp
// Request
class SNAPSER_API SnapserTrackablesServiceApi::TrackablesConsumeUserEnergyRequest : public Request
{
public:
    virtual ~TrackablesConsumeUserEnergyRequest() {}
	void SetupHttpRequest(const FHttpRequestRef& HttpRequest) const final;
	FString ComputePath() const final;

	/* UserID of the affected user */
	FString UserId;
	/* Name of energy to consume */
	FString EnergyName;
	/* User session token */
	FString Token;
	SnapserTrackablesServiceConsumeUserEnergyBody Body;
};

class SNAPSER_API SnapserTrackablesServiceConsumeUserEnergyBody : public Model
{
public:
    virtual ~SnapserTrackablesServiceConsumeUserEnergyBody() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Value to decrement the energy by */
	int64 Delta = 0;
};

// Response
class SNAPSER_API SnapserTrackablesServiceApi::TrackablesConsumeUserEnergyResponse : public Response
{
public:
    virtual ~TrackablesConsumeUserEnergyResponse() {}
	void SetHttpResponseCode(EHttpResponseCodes::Type InHttpResponseCode) final;
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;

    SnapserTrackablesUpdateUserEnergyInfo Content;
};

class SNAPSER_API SnapserTrackablesUpdateUserEnergyInfo : public Model
{
public:
    virtual ~SnapserTrackablesUpdateUserEnergyInfo() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Energy of user after the update */
	TOptional<int32> AfterEnergy;
	/* Energy of user before the update */
	TOptional<int32> BeforeEnergy;
	/* Energy of the user. (DEPRECATED: Use after_energy) */
	TOptional<int32> Energy;
	/* Metadata associated with the energy setting */
	TOptional<TSharedPtr<FJsonObject>> Metadata;
	/* Next energy at Timestamp */
	TOptional<int64> NextEnergyAt;
	/* Seconds to next energy */
	TOptional<int32> SecondsToNextEnergy;
	/* Status of energy update, true if energy updated successfully */
	TOptional<bool> Success;
	/* UserID of the user */
	TOptional<FString> UserId;
};

```
```go
// Coming soon
```
```python
# Coming soon
```
</CodeGroup>
#### TrackablesIncrementUserEnergy API
<CodeGroup title="Increments the energy of the user" tag="PATCH" label="TrackablesIncrementUserEnergy">
```csharp
/// <summary>
/// Increment user energy Increments the energy of the user (App auth only)
/// </summary>
/// <exception cref="Snapser.Client.ApiException">Thrown when fails to make API call</exception>
/// <param name="userId">UserID of the affected user</param>
/// <param name="energyName">Name of energy to increment</param>
/// <param name="token">User session token</param>
/// <param name="body"></param>
/// <returns>TrackablesUpdateUserEnergyInfo</returns>
public TrackablesUpdateUserEnergyInfo TrackablesIncrementUserEnergy(string userId, string energyName, string token, TrackablesServiceIncrementUserEnergyBody body)
{
    Snapser.Client.ApiResponse<TrackablesUpdateUserEnergyInfo> localVarResponse = TrackablesIncrementUserEnergyWithHttpInfo(userId, energyName, token, body);
    return localVarResponse.Data;
}

/// Snapser.Model for TrackablesServiceIncrementUserEnergyBody
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesServiceIncrementUserEnergyBody" /> class.
/// </summary>
/// <param name="delta">Value to decrement the energy by (required).</param>
public TrackablesServiceIncrementUserEnergyBody(long delta = default(long))
{
    this.Delta = delta;
}

/// Snapser.Model for TrackablesUpdateUserEnergyInfo
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesUpdateUserEnergyInfo" /> class.
/// </summary>
/// <param name="afterEnergy">Energy of user after the update.</param>
/// <param name="beforeEnergy">Energy of user before the update.</param>
/// <param name="energy">Energy of the user. (DEPRECATED: Use after_energy).</param>
/// <param name="metadata">Metadata associated with the energy setting.</param>
/// <param name="nextEnergyAt">Next energy at Timestamp.</param>
/// <param name="secondsToNextEnergy">Seconds to next energy.</param>
/// <param name="success">Status of energy update, true if energy updated successfully.</param>
/// <param name="userId">UserID of the user.</param>
public TrackablesUpdateUserEnergyInfo(int afterEnergy = default(int), int beforeEnergy = default(int), int energy = default(int), Object metadata = default(Object), long nextEnergyAt = default(long), int secondsToNextEnergy = default(int), bool success = default(bool), string userId = default(string))
{
	this.AfterEnergy = afterEnergy;
	this.BeforeEnergy = beforeEnergy;
	this.Energy = energy;
	this.Metadata = metadata;
	this.NextEnergyAt = nextEnergyAt;
	this.SecondsToNextEnergy = secondsToNextEnergy;
	this.Success = success;
	this.UserId = userId;
}
```
```cpp
// Request
class SNAPSER_API SnapserTrackablesServiceApi::TrackablesIncrementUserEnergyRequest : public Request
{
public:
    virtual ~TrackablesIncrementUserEnergyRequest() {}
	void SetupHttpRequest(const FHttpRequestRef& HttpRequest) const final;
	FString ComputePath() const final;

	/* UserID of the affected user */
	FString UserId;
	/* Name of energy to increment */
	FString EnergyName;
	/* User session token */
	FString Token;
	SnapserTrackablesServiceIncrementUserEnergyBody Body;
};

class SNAPSER_API SnapserTrackablesServiceIncrementUserEnergyBody : public Model
{
public:
    virtual ~SnapserTrackablesServiceIncrementUserEnergyBody() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Value to decrement the energy by */
	int64 Delta = 0;
};

// Response
class SNAPSER_API SnapserTrackablesServiceApi::TrackablesIncrementUserEnergyResponse : public Response
{
public:
    virtual ~TrackablesIncrementUserEnergyResponse() {}
	void SetHttpResponseCode(EHttpResponseCodes::Type InHttpResponseCode) final;
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;

    SnapserTrackablesUpdateUserEnergyInfo Content;
};

class SNAPSER_API SnapserTrackablesUpdateUserEnergyInfo : public Model
{
public:
    virtual ~SnapserTrackablesUpdateUserEnergyInfo() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Energy of user after the update */
	TOptional<int32> AfterEnergy;
	/* Energy of user before the update */
	TOptional<int32> BeforeEnergy;
	/* Energy of the user. (DEPRECATED: Use after_energy) */
	TOptional<int32> Energy;
	/* Metadata associated with the energy setting */
	TOptional<TSharedPtr<FJsonObject>> Metadata;
	/* Next energy at Timestamp */
	TOptional<int64> NextEnergyAt;
	/* Seconds to next energy */
	TOptional<int32> SecondsToNextEnergy;
	/* Status of energy update, true if energy updated successfully */
	TOptional<bool> Success;
	/* UserID of the user */
	TOptional<FString> UserId;
};

```
```go
// Coming soon
```
```python
# Coming soon
```
</CodeGroup>
#### TrackablesGetUserXp API
<CodeGroup title="Gets the xp of given user, returns error if not set" tag="GET" label="TrackablesGetUserXp">
```csharp
/// <summary>
/// User XP Gets the xp of given user, returns error if not set
/// </summary>
/// <exception cref="Snapser.Client.ApiException">Thrown when fails to make API call</exception>
/// <param name="userId">UserID of user</param>
/// <param name="name">Name of xp</param>
/// <param name="token">User session token</param>
/// <returns>TrackablesGetUserXpResponse</returns>
public TrackablesGetUserXpResponse TrackablesGetUserXp(string userId, string name, string token)
{
	Snapser.Client.ApiResponse<TrackablesGetUserXpResponse> localVarResponse = TrackablesGetUserXpWithHttpInfo(userId, name, token);
	return localVarResponse.Data;
}

/// Snapser.Model for TrackablesGetUserXpResponse
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesGetUserXpResponse" /> class.
/// </summary>
/// <param name="currentLevel">currentLevel.</param>
/// <param name="metadata">Metadata associated with the xp setting.</param>
/// <param name="nextLevels">Up to next 3 levels in progression.</param>
/// <param name="xp">Xp of the user.</param>
public TrackablesGetUserXpResponse(TrackablesLevel currentLevel = default(TrackablesLevel), Object metadata = default(Object), List<TrackablesLevel> nextLevels = default(List<TrackablesLevel>), long xp = default(long))
{
	this.CurrentLevel = currentLevel;
	this.Metadata = metadata;
	this.NextLevels = nextLevels;
	this.Xp = xp;
}

/// Snapser.Model for TrackablesLevel
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesLevel" /> class.
/// </summary>
/// <param name="end">Ending xp for the level.</param>
/// <param name="index">Index of the level in the xp setting.</param>
/// <param name="levelCompletionRewards">levelCompletionRewards.</param>
/// <param name="levelUpRewards">levelUpRewards.</param>
/// <param name="metadata">Metadata associated with the level.</param>
/// <param name="name">Name of the level.</param>
/// <param name="rewardCurrencies">Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardCurrencies64">64-bit Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardEnergies">Energies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardInventoryItems">Inventory items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardItems">Items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="start">Starting xp for the level.</param>
public TrackablesLevel(long end = default(long), int index = default(int), TrackablesRewards levelCompletionRewards = default(TrackablesRewards), TrackablesRewards levelUpRewards = default(TrackablesRewards), Object metadata = default(Object), string name = default(string), Dictionary<string, int> rewardCurrencies = default(Dictionary<string, int>), Dictionary<string, string> rewardCurrencies64 = default(Dictionary<string, string>), Dictionary<string, int> rewardEnergies = default(Dictionary<string, int>), Dictionary<string, TrackablesRewardInventoryItems> rewardInventoryItems = default(Dictionary<string, TrackablesRewardInventoryItems>), Dictionary<string, int> rewardItems = default(Dictionary<string, int>), long start = default(long))
{
	this.End = end;
	this.Index = index;
	this.LevelCompletionRewards = levelCompletionRewards;
	this.LevelUpRewards = levelUpRewards;
	this.Metadata = metadata;
	this.Name = name;
	this.RewardCurrencies = rewardCurrencies;
	this.RewardCurrencies64 = rewardCurrencies64;
	this.RewardEnergies = rewardEnergies;
	this.RewardInventoryItems = rewardInventoryItems;
	this.RewardItems = rewardItems;
	this.Start = start;
}

/// Snapser.Model for TrackablesRewards
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesRewards" /> class.
/// </summary>
/// <param name="currencies">Currencies to be rewarded.</param>
/// <param name="dropTables">Drop tables to grant.</param>
/// <param name="energies">Energies to be rewarded.</param>
/// <param name="items">Items to be rewarded.</param>
public TrackablesRewards(Dictionary<string, string> currencies = default, List<TrackablesRewardDropTable> dropTables = default, Dictionary<string, int> energies = default, Dictionary<string, TrackablesRewardInventoryItems> items = default)
{
	this.Currencies = currencies;
	this.DropTables = dropTables;
	this.Energies = energies;
	this.Items = items;
}

/// Snapser.Model for TrackablesRewardInventoryItems
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesRewardInventoryItems" /> class.
/// </summary>
/// <param name="count">Count of the inventory item to be rewarded.</param>
/// <param name="subInventoryItemName">Sub-inventory item name, if applicable.</param>
public TrackablesRewardInventoryItems(int count = default(int), string subInventoryItemName = default(string))
{
	this.Count = count;
	this.SubInventoryItemName = subInventoryItemName;
}

/// Snapser.Model for TrackablesRewardDropTable
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesRewardDropTable" /> class.
/// </summary>
/// <param name="count">Number of times to evaluate the drop table (default 1).</param>
/// <param name="name">Name of the drop table.</param>
public TrackablesRewardDropTable(int count = default, string name = default)
{
	this.Count = count;
	this.Name = name;
}
```
```cpp
// Request
class SNAPSER_API SnapserTrackablesServiceApi::TrackablesGetUserXpRequest : public Request
{
public:
    virtual ~TrackablesGetUserXpRequest() {}
	void SetupHttpRequest(const FHttpRequestRef& HttpRequest) const final;
	FString ComputePath() const final;

	/* UserID of user */
	FString UserId;
	/* Name of xp */
	FString Name;
	/* User session token */
	FString Token;
};

// Response
class SNAPSER_API SnapserTrackablesServiceApi::TrackablesGetUserXpResponse : public Response
{
public:
    virtual ~TrackablesGetUserXpResponse() {}
	void SetHttpResponseCode(EHttpResponseCodes::Type InHttpResponseCode) final;
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;

    SnapserTrackablesGetUserXpResponse Content;
};

class SNAPSER_API SnapserTrackablesGetUserXpResponse : public Model
{
public:
    virtual ~SnapserTrackablesGetUserXpResponse() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	TOptional<SnapserTrackablesLevel> CurrentLevel;
	/* Metadata associated with the xp setting */
	TOptional<TSharedPtr<FJsonObject>> Metadata;
	/* Up to next 3 levels in progression */
	TOptional<TArray<SnapserTrackablesLevel>> NextLevels;
	/* Xp of the user */
	TOptional<int64> Xp;
};

class SNAPSER_API SnapserTrackablesLevel : public Model
{
public:
    virtual ~SnapserTrackablesLevel() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Ending xp for the level */
	TOptional<int64> End;
	/* Index of the level in the xp setting */
	TOptional<int32> Index;
	TOptional<SnapserTrackablesRewards> LevelCompletionRewards;
	TOptional<SnapserTrackablesRewards> LevelUpRewards;
	/* Metadata associated with the level */
	TOptional<TSharedPtr<FJsonObject>> Metadata;
	/* Name of the level */
	TOptional<FString> Name;
	/* Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, int32>> RewardCurrencies;
	/* 64-bit Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, FString>> RewardCurrencies64;
	/* Energies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, int32>> RewardEnergies;
	/* Inventory items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, SnapserTrackablesRewardInventoryItems>> RewardInventoryItems;
	/* Items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, int32>> RewardItems;
	/* Starting xp for the level */
	TOptional<int64> Start;
};

class SNAPSER_API SnapserTrackablesRewards : public Model
{
public:
    virtual ~SnapserTrackablesRewards() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Currencies to be rewarded */
	TOptional<TMap<FString, FString>> Currencies;
	/* Drop tables to grant */
	TOptional<TArray<SnapserTrackablesRewardDropTable>> DropTables;
	/* Energies to be rewarded */
	TOptional<TMap<FString, int32>> Energies;
	/* Items to be rewarded */
	TOptional<TMap<FString, SnapserTrackablesRewardInventoryItems>> Items;
};

class SNAPSER_API SnapserTrackablesRewardDropTable : public Model
{
public:
    virtual ~SnapserTrackablesRewardDropTable() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Number of times to evaluate the drop table (default 1) */
	TOptional<int32> Count;
	/* Name of the drop table */
	TOptional<FString> Name;
};

class SNAPSER_API SnapserTrackablesRewardInventoryItems : public Model
{
public:
    virtual ~SnapserTrackablesRewardInventoryItems() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Count of the inventory item to be rewarded */
	TOptional<int32> Count;
	/* Sub-inventory item name, if applicable */
	TOptional<FString> SubInventoryItemName;
};

```
```go
// Coming soon
```
```python
# Coming soon
```
</CodeGroup>
#### TrackablesIncrementUserXp API
<CodeGroup title="Increments the xp of given user, starts from zero if a user xp is not set" tag="PATCH" label="TrackablesIncrementUserXp">
```csharp
/// <summary>
/// User XP Increments the xp of given user, starts from zero if a user xp is not set
/// </summary>
/// <exception cref="Snapser.Client.ApiException">Thrown when fails to make API call</exception>
/// <param name="userId">UserID of the affected user</param>
/// <param name="name">Name of xp to increment</param>
/// <param name="token">User session token</param>
/// <param name="body"></param>
/// <returns>TrackablesUpdateUserXpInfo</returns>
public TrackablesUpdateUserXpInfo TrackablesIncrementUserXp(string userId, string name, string token, IncrementUserXpRequest body)
{
	Snapser.Client.ApiResponse<TrackablesUpdateUserXpInfo> localVarResponse = TrackablesIncrementUserXpWithHttpInfo(userId, name, token, body);
	return localVarResponse.Data;
}

/// Snapser.Model for IncrementUserXpRequest
/// <summary>
/// Initializes a new instance of the <see cref="IncrementUserXpRequest" /> class.
/// </summary>
/// <param name="delta">Value to increment the statistic xp by. Should be non negative integer (required).</param>
public IncrementUserXpRequest(long delta = default(long))
{
	this.Delta = delta;
}

/// Snapser.Model for TrackablesUpdateUserXpInfo
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesUpdateUserXpInfo" /> class.
/// </summary>
/// <param name="afterXp">Xp of user before the update.</param>
/// <param name="beforeXp">Xp of user before the update.</param>
/// <param name="currentLevel">currentLevel.</param>
/// <param name="levelCompletionRewards">Array of reward grant statuses for level completion rewards for each level progressed.</param>
/// <param name="levelUp">Boolean indicating if the user have levelled up because of update.</param>
/// <param name="levelUpRewards">Array of reward grant statuses for level up rewards for each level progressed.</param>
/// <param name="levelsProgressed">Array of levels progressed by the user in the event of level up.</param>
/// <param name="metadata">Metadata associated with the xp setting.</param>
/// <param name="nextLevels">Up to next 3 levels in progression.</param>
/// <param name="overflowXp">Remaining Xp after final level reached.</param>
/// <param name="rewardGrantStatuses">Array of reward grant statuses for each level progressed (DEPRECATED: Use level_completion_rewards, or level_up_rewards).</param>
/// <param name="rewardsGrantStatusPerLevel">Boolean indicator of rewards granted per level progressed (Deprecated: Use level_completion_rewards, or level_up_rewards).</param>
/// <param name="rewardsGranted">Boolean indicating if the user have been granted rewards successfully for all the levels progressed.</param>
/// <param name="success">Status of xp update, true if xp updated successfully.</param>
/// <param name="userId">UserID of the user.</param>
public TrackablesUpdateUserXpInfo(long afterXp = default(long), long beforeXp = default(long), TrackablesLevel currentLevel = default(TrackablesLevel), List<TrackablesRewardGrantStatus> levelCompletionRewards = default(List<TrackablesRewardGrantStatus>), bool levelUp = default(bool), List<TrackablesRewardGrantStatus> levelUpRewards = default(List<TrackablesRewardGrantStatus>), List<TrackablesLevel> levelsProgressed = default(List<TrackablesLevel>), Object metadata = default(Object), List<TrackablesLevel> nextLevels = default(List<TrackablesLevel>), long overflowXp = default(long), List<TrackablesRewardGrantStatus> rewardGrantStatuses = default(List<TrackablesRewardGrantStatus>), Dictionary<string, bool> rewardsGrantStatusPerLevel = default(Dictionary<string, bool>), bool rewardsGranted = default(bool), bool success = default(bool), string userId = default(string))
{
	this.AfterXp = afterXp;
	this.BeforeXp = beforeXp;
	this.CurrentLevel = currentLevel;
	this.LevelCompletionRewards = levelCompletionRewards;
	this.LevelUp = levelUp;
	this.LevelUpRewards = levelUpRewards;
	this.LevelsProgressed = levelsProgressed;
	this.Metadata = metadata;
	this.NextLevels = nextLevels;
	this.OverflowXp = overflowXp;
	this.RewardGrantStatuses = rewardGrantStatuses;
	this.RewardsGrantStatusPerLevel = rewardsGrantStatusPerLevel;
	this.RewardsGranted = rewardsGranted;
	this.Success = success;
	this.UserId = userId;
}

/// Snapser.Model for TrackablesRewardGrantStatus
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesRewardGrantStatus" /> class.
/// </summary>
/// <param name="dropTableResults">Results from drop table evaluations.</param>
/// <param name="failedDropTables">Drop tables that failed to grant.</param>
/// <param name="levelName">Name of the level for which the reward was granted.</param>
/// <param name="status">status.</param>
/// <param name="success">Indicates if the reward was granted successfully. Deprecated: Use status field.</param>
public TrackablesRewardGrantStatus(List<TrackablesDropTableGrantResult> dropTableResults = default, List<string> failedDropTables = default, string levelName = default, TrackablesRewardGrantStatusStatus? status = default, bool success = default)
{
	this.DropTableResults = dropTableResults;
	this.FailedDropTables = failedDropTables;
	this.LevelName = levelName;
	this.Status = status;
	this.Success = success;
}

/// Snapser.Model for TrackablesDropTableGrantResult
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesDropTableGrantResult" /> class.
/// </summary>
/// <param name="currenciesGranted">Currencies granted from this drop table evaluation.</param>
/// <param name="dropIndex">Index of the drop that was selected.</param>
/// <param name="dropTableName">Name of the drop table that was evaluated.</param>
/// <param name="energyGranted">Energy granted from this drop table evaluation.</param>
/// <param name="itemsGranted">Items granted from this drop table evaluation.</param>
public TrackablesDropTableGrantResult(Dictionary<string, string> currenciesGranted = default, int dropIndex = default, string dropTableName = default, Dictionary<string, int> energyGranted = default, Dictionary<string, int> itemsGranted = default)
{
	this.CurrenciesGranted = currenciesGranted;
	this.DropIndex = dropIndex;
	this.DropTableName = dropTableName;
	this.EnergyGranted = energyGranted;
	this.ItemsGranted = itemsGranted;
}

/// Snapser.Model for TrackablesLevel
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesLevel" /> class.
/// </summary>
/// <param name="end">Ending xp for the level.</param>
/// <param name="index">Index of the level in the xp setting.</param>
/// <param name="levelCompletionRewards">levelCompletionRewards.</param>
/// <param name="levelUpRewards">levelUpRewards.</param>
/// <param name="metadata">Metadata associated with the level.</param>
/// <param name="name">Name of the level.</param>
/// <param name="rewardCurrencies">Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardCurrencies64">64-bit Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardEnergies">Energies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardInventoryItems">Inventory items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardItems">Items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="start">Starting xp for the level.</param>
public TrackablesLevel(long end = default(long), int index = default(int), TrackablesRewards levelCompletionRewards = default(TrackablesRewards), TrackablesRewards levelUpRewards = default(TrackablesRewards), Object metadata = default(Object), string name = default(string), Dictionary<string, int> rewardCurrencies = default(Dictionary<string, int>), Dictionary<string, string> rewardCurrencies64 = default(Dictionary<string, string>), Dictionary<string, int> rewardEnergies = default(Dictionary<string, int>), Dictionary<string, TrackablesRewardInventoryItems> rewardInventoryItems = default(Dictionary<string, TrackablesRewardInventoryItems>), Dictionary<string, int> rewardItems = default(Dictionary<string, int>), long start = default(long))
{
	this.End = end;
	this.Index = index;
	this.LevelCompletionRewards = levelCompletionRewards;
	this.LevelUpRewards = levelUpRewards;
	this.Metadata = metadata;
	this.Name = name;
	this.RewardCurrencies = rewardCurrencies;
	this.RewardCurrencies64 = rewardCurrencies64;
	this.RewardEnergies = rewardEnergies;
	this.RewardInventoryItems = rewardInventoryItems;
	this.RewardItems = rewardItems;
	this.Start = start;
}

/// Snapser.Model for TrackablesRewards
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesRewards" /> class.
/// </summary>
/// <param name="currencies">Currencies to be rewarded.</param>
/// <param name="dropTables">Drop tables to grant.</param>
/// <param name="energies">Energies to be rewarded.</param>
/// <param name="items">Items to be rewarded.</param>
public TrackablesRewards(Dictionary<string, string> currencies = default, List<TrackablesRewardDropTable> dropTables = default, Dictionary<string, int> energies = default, Dictionary<string, TrackablesRewardInventoryItems> items = default)
{
	this.Currencies = currencies;
	this.DropTables = dropTables;
	this.Energies = energies;
	this.Items = items;
}

/// Snapser.Model for TrackablesRewardInventoryItems
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesRewardInventoryItems" /> class.
/// </summary>
/// <param name="count">Count of the inventory item to be rewarded.</param>
/// <param name="subInventoryItemName">Sub-inventory item name, if applicable.</param>
public TrackablesRewardInventoryItems(int count = default(int), string subInventoryItemName = default(string))
{
	this.Count = count;
	this.SubInventoryItemName = subInventoryItemName;
}

/// Snapser.Model for TrackablesRewardDropTable
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesRewardDropTable" /> class.
/// </summary>
/// <param name="count">Number of times to evaluate the drop table (default 1).</param>
/// <param name="name">Name of the drop table.</param>
public TrackablesRewardDropTable(int count = default, string name = default)
{
	this.Count = count;
	this.Name = name;
}
```
```cpp
// Request
class SNAPSER_API SnapserTrackablesServiceApi::TrackablesIncrementUserXpRequest : public Request
{
public:
    virtual ~TrackablesIncrementUserXpRequest() {}
	void SetupHttpRequest(const FHttpRequestRef& HttpRequest) const final;
	FString ComputePath() const final;

	/* UserID of the affected user */
	FString UserId;
	/* Name of xp to increment */
	FString Name;
	/* User session token */
	FString Token;
	SnapserIncrementUserXpRequest Body;
};

class SNAPSER_API SnapserIncrementUserXpRequest : public Model
{
public:
    virtual ~SnapserIncrementUserXpRequest() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Value to increment the statistic xp by. Should be non negative integer */
	int64 Delta = 0;
};

// Response
class SNAPSER_API SnapserTrackablesServiceApi::TrackablesIncrementUserXpResponse : public Response
{
public:
    virtual ~TrackablesIncrementUserXpResponse() {}
	void SetHttpResponseCode(EHttpResponseCodes::Type InHttpResponseCode) final;
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;

    SnapserTrackablesUpdateUserXpInfo Content;
};

class SNAPSER_API SnapserTrackablesUpdateUserXpInfo : public Model
{
public:
    virtual ~SnapserTrackablesUpdateUserXpInfo() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Xp of user before the update */
	TOptional<int64> AfterXp;
	/* Xp of user before the update */
	TOptional<int64> BeforeXp;
	TOptional<SnapserTrackablesLevel> CurrentLevel;
	/* Array of reward grant statuses for level completion rewards for each level progressed */
	TOptional<TArray<SnapserTrackablesRewardGrantStatus>> LevelCompletionRewards;
	/* Boolean indicating if the user have levelled up because of update */
	TOptional<bool> LevelUp;
	/* Array of reward grant statuses for level up rewards for each level progressed */
	TOptional<TArray<SnapserTrackablesRewardGrantStatus>> LevelUpRewards;
	/* Array of levels progressed by the user in the event of level up */
	TOptional<TArray<SnapserTrackablesLevel>> LevelsProgressed;
	/* Metadata associated with the xp setting */
	TOptional<TSharedPtr<FJsonObject>> Metadata;
	/* Up to next 3 levels in progression */
	TOptional<TArray<SnapserTrackablesLevel>> NextLevels;
	/* Remaining Xp after final level reached */
	TOptional<int64> OverflowXp;
	/* Array of reward grant statuses for each level progressed (DEPRECATED: Use level_completion_rewards, or level_up_rewards) */
	TOptional<TArray<SnapserTrackablesRewardGrantStatus>> RewardGrantStatuses;
	/* Boolean indicator of rewards granted per level progressed (Deprecated: Use level_completion_rewards, or level_up_rewards) */
	TOptional<TMap<FString, bool>> RewardsGrantStatusPerLevel;
	/* Boolean indicating if the user have been granted rewards successfully for all the levels progressed */
	TOptional<bool> RewardsGranted;
	/* Status of xp update, true if xp updated successfully */
	TOptional<bool> Success;
	/* UserID of the user */
	TOptional<FString> UserId;
};

class SNAPSER_API SnapserTrackablesRewardGrantStatus : public Model
{
public:
    virtual ~SnapserTrackablesRewardGrantStatus() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Results from drop table evaluations */
	TOptional<TArray<SnapserTrackablesDropTableGrantResult>> DropTableResults;
	/* Drop tables that failed to grant */
	TOptional<TArray<FString>> FailedDropTables;
	/* Name of the level for which the reward was granted */
	TOptional<FString> LevelName;
	TOptional<SnapserTrackablesRewardGrantStatusStatus> Status;
	/* Indicates if the reward was granted successfully. Deprecated: Use status field */
	TOptional<bool> Success;
};

class SNAPSER_API SnapserTrackablesDropTableGrantResult : public Model
{
public:
    virtual ~SnapserTrackablesDropTableGrantResult() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Currencies granted from this drop table evaluation */
	TOptional<TMap<FString, FString>> CurrenciesGranted;
	/* Index of the drop that was selected */
	TOptional<int32> DropIndex;
	/* Name of the drop table that was evaluated */
	TOptional<FString> DropTableName;
	/* Energy granted from this drop table evaluation */
	TOptional<TMap<FString, int32>> EnergyGranted;
	/* Items granted from this drop table evaluation */
	TOptional<TMap<FString, int32>> ItemsGranted;
};

class SNAPSER_API SnapserTrackablesRewardGrantStatusStatus : public Model
{
public:
    virtual ~SnapserTrackablesRewardGrantStatusStatus() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	enum class Values
	{
		UnknownStatus,
		Granted,
		Failed,
		NotConfigured,
		Skipped,
  	};

	Values Value;

	static FString EnumToString(const Values& EnumValue);
	static bool EnumFromString(const FString& EnumAsString, Values& EnumValue);
};

class SNAPSER_API SnapserTrackablesLevel : public Model
{
public:
    virtual ~SnapserTrackablesLevel() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Ending xp for the level */
	TOptional<int64> End;
	/* Index of the level in the xp setting */
	TOptional<int32> Index;
	TOptional<SnapserTrackablesRewards> LevelCompletionRewards;
	TOptional<SnapserTrackablesRewards> LevelUpRewards;
	/* Metadata associated with the level */
	TOptional<TSharedPtr<FJsonObject>> Metadata;
	/* Name of the level */
	TOptional<FString> Name;
	/* Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, int32>> RewardCurrencies;
	/* 64-bit Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, FString>> RewardCurrencies64;
	/* Energies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, int32>> RewardEnergies;
	/* Inventory items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, SnapserTrackablesRewardInventoryItems>> RewardInventoryItems;
	/* Items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, int32>> RewardItems;
	/* Starting xp for the level */
	TOptional<int64> Start;
};

class SNAPSER_API SnapserTrackablesRewards : public Model
{
public:
    virtual ~SnapserTrackablesRewards() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Currencies to be rewarded */
	TOptional<TMap<FString, FString>> Currencies;
	/* Drop tables to grant */
	TOptional<TArray<SnapserTrackablesRewardDropTable>> DropTables;
	/* Energies to be rewarded */
	TOptional<TMap<FString, int32>> Energies;
	/* Items to be rewarded */
	TOptional<TMap<FString, SnapserTrackablesRewardInventoryItems>> Items;
};

class SNAPSER_API SnapserTrackablesRewardDropTable : public Model
{
public:
    virtual ~SnapserTrackablesRewardDropTable() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Number of times to evaluate the drop table (default 1) */
	TOptional<int32> Count;
	/* Name of the drop table */
	TOptional<FString> Name;
};

class SNAPSER_API SnapserTrackablesRewardInventoryItems : public Model
{
public:
    virtual ~SnapserTrackablesRewardInventoryItems() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Count of the inventory item to be rewarded */
	TOptional<int32> Count;
	/* Sub-inventory item name, if applicable */
	TOptional<FString> SubInventoryItemName;
};

```
```go
// Coming soon
```
```python
# Coming soon
```
</CodeGroup>
#### TrackablesSetUserXp API
<CodeGroup title="Sets the xp for a given user" tag="PUT" label="TrackablesSetUserXp">
```csharp
/// <summary>
/// User XP (App Auth) Sets the xp of given users
/// </summary>
/// <exception cref="Snapser.Client.ApiException">Thrown when fails to make API call</exception>
/// <param name="userId">UserID of the affected user</param>
/// <param name="name">Name of xp to set</param>
/// <param name="token">User session token</param>
/// <param name="body"></param>
/// <returns>TrackablesUpdateUserXpInfo</returns>
public TrackablesUpdateUserXpInfo TrackablesSetUserXp(string userId, string name, string token, SetUserXpRequest body)
{
	Snapser.Client.ApiResponse<TrackablesUpdateUserXpInfo> localVarResponse = TrackablesSetUserXpWithHttpInfo(userId, name, token, body);
	return localVarResponse.Data;
}

/// Snapser.Model for TrackablesSetUserXpRequest
/// <summary>
/// Initializes a new instance of the <see cref="SetUserXpRequest" /> class.
/// </summary>
/// <param name="value">Value to be set as the user xp statistic (required).</param>
public SetUserXpRequest(long value = default(long))
{
	this.Value = value;
}

/// Snapser.Model for TrackablesUpdateUserXpInfo
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesUpdateUserXpInfo" /> class.
/// </summary>
/// <param name="afterXp">Xp of user before the update.</param>
/// <param name="beforeXp">Xp of user before the update.</param>
/// <param name="currentLevel">currentLevel.</param>
/// <param name="levelCompletionRewards">Array of reward grant statuses for level completion rewards for each level progressed.</param>
/// <param name="levelUp">Boolean indicating if the user have levelled up because of update.</param>
/// <param name="levelUpRewards">Array of reward grant statuses for level up rewards for each level progressed.</param>
/// <param name="levelsProgressed">Array of levels progressed by the user in the event of level up.</param>
/// <param name="metadata">Metadata associated with the xp setting.</param>
/// <param name="nextLevels">Up to next 3 levels in progression.</param>
/// <param name="overflowXp">Remaining Xp after final level reached.</param>
/// <param name="rewardGrantStatuses">Array of reward grant statuses for each level progressed (DEPRECATED: Use level_completion_rewards, or level_up_rewards).</param>
/// <param name="rewardsGrantStatusPerLevel">Boolean indicator of rewards granted per level progressed (Deprecated: Use level_completion_rewards, or level_up_rewards).</param>
/// <param name="rewardsGranted">Boolean indicating if the user have been granted rewards successfully for all the levels progressed.</param>
/// <param name="success">Status of xp update, true if xp updated successfully.</param>
/// <param name="userId">UserID of the user.</param>
public TrackablesUpdateUserXpInfo(long afterXp = default(long), long beforeXp = default(long), TrackablesLevel currentLevel = default(TrackablesLevel), List<TrackablesRewardGrantStatus> levelCompletionRewards = default(List<TrackablesRewardGrantStatus>), bool levelUp = default(bool), List<TrackablesRewardGrantStatus> levelUpRewards = default(List<TrackablesRewardGrantStatus>), List<TrackablesLevel> levelsProgressed = default(List<TrackablesLevel>), Object metadata = default(Object), List<TrackablesLevel> nextLevels = default(List<TrackablesLevel>), long overflowXp = default(long), List<TrackablesRewardGrantStatus> rewardGrantStatuses = default(List<TrackablesRewardGrantStatus>), Dictionary<string, bool> rewardsGrantStatusPerLevel = default(Dictionary<string, bool>), bool rewardsGranted = default(bool), bool success = default(bool), string userId = default(string))
{
	this.AfterXp = afterXp;
	this.BeforeXp = beforeXp;
	this.CurrentLevel = currentLevel;
	this.LevelCompletionRewards = levelCompletionRewards;
	this.LevelUp = levelUp;
	this.LevelUpRewards = levelUpRewards;
	this.LevelsProgressed = levelsProgressed;
	this.Metadata = metadata;
	this.NextLevels = nextLevels;
	this.OverflowXp = overflowXp;
	this.RewardGrantStatuses = rewardGrantStatuses;
	this.RewardsGrantStatusPerLevel = rewardsGrantStatusPerLevel;
	this.RewardsGranted = rewardsGranted;
	this.Success = success;
	this.UserId = userId;
}

/// Snapser.Model for TrackablesRewardGrantStatus
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesRewardGrantStatus" /> class.
/// </summary>
/// <param name="dropTableResults">Results from drop table evaluations.</param>
/// <param name="failedDropTables">Drop tables that failed to grant.</param>
/// <param name="levelName">Name of the level for which the reward was granted.</param>
/// <param name="status">status.</param>
/// <param name="success">Indicates if the reward was granted successfully. Deprecated: Use status field.</param>
public TrackablesRewardGrantStatus(List<TrackablesDropTableGrantResult> dropTableResults = default, List<string> failedDropTables = default, string levelName = default, TrackablesRewardGrantStatusStatus? status = default, bool success = default)
{
	this.DropTableResults = dropTableResults;
	this.FailedDropTables = failedDropTables;
	this.LevelName = levelName;
	this.Status = status;
	this.Success = success;
}

/// Snapser.Model for TrackablesDropTableGrantResult
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesDropTableGrantResult" /> class.
/// </summary>
/// <param name="currenciesGranted">Currencies granted from this drop table evaluation.</param>
/// <param name="dropIndex">Index of the drop that was selected.</param>
/// <param name="dropTableName">Name of the drop table that was evaluated.</param>
/// <param name="energyGranted">Energy granted from this drop table evaluation.</param>
/// <param name="itemsGranted">Items granted from this drop table evaluation.</param>
public TrackablesDropTableGrantResult(Dictionary<string, string> currenciesGranted = default, int dropIndex = default, string dropTableName = default, Dictionary<string, int> energyGranted = default, Dictionary<string, int> itemsGranted = default)
{
	this.CurrenciesGranted = currenciesGranted;
	this.DropIndex = dropIndex;
	this.DropTableName = dropTableName;
	this.EnergyGranted = energyGranted;
	this.ItemsGranted = itemsGranted;
}

/// Snapser.Model for TrackablesLevel
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesLevel" /> class.
/// </summary>
/// <param name="end">Ending xp for the level.</param>
/// <param name="index">Index of the level in the xp setting.</param>
/// <param name="levelCompletionRewards">levelCompletionRewards.</param>
/// <param name="levelUpRewards">levelUpRewards.</param>
/// <param name="metadata">Metadata associated with the level.</param>
/// <param name="name">Name of the level.</param>
/// <param name="rewardCurrencies">Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardCurrencies64">64-bit Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardEnergies">Energies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardInventoryItems">Inventory items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardItems">Items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="start">Starting xp for the level.</param>
public TrackablesLevel(long end = default(long), int index = default(int), TrackablesRewards levelCompletionRewards = default(TrackablesRewards), TrackablesRewards levelUpRewards = default(TrackablesRewards), Object metadata = default(Object), string name = default(string), Dictionary<string, int> rewardCurrencies = default(Dictionary<string, int>), Dictionary<string, string> rewardCurrencies64 = default(Dictionary<string, string>), Dictionary<string, int> rewardEnergies = default(Dictionary<string, int>), Dictionary<string, TrackablesRewardInventoryItems> rewardInventoryItems = default(Dictionary<string, TrackablesRewardInventoryItems>), Dictionary<string, int> rewardItems = default(Dictionary<string, int>), long start = default(long))
{
	this.End = end;
	this.Index = index;
	this.LevelCompletionRewards = levelCompletionRewards;
	this.LevelUpRewards = levelUpRewards;
	this.Metadata = metadata;
	this.Name = name;
	this.RewardCurrencies = rewardCurrencies;
	this.RewardCurrencies64 = rewardCurrencies64;
	this.RewardEnergies = rewardEnergies;
	this.RewardInventoryItems = rewardInventoryItems;
	this.RewardItems = rewardItems;
	this.Start = start;
}

/// Snapser.Model for TrackablesRewards
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesRewards" /> class.
/// </summary>
/// <param name="currencies">Currencies to be rewarded.</param>
/// <param name="dropTables">Drop tables to grant.</param>
/// <param name="energies">Energies to be rewarded.</param>
/// <param name="items">Items to be rewarded.</param>
public TrackablesRewards(Dictionary<string, string> currencies = default, List<TrackablesRewardDropTable> dropTables = default, Dictionary<string, int> energies = default, Dictionary<string, TrackablesRewardInventoryItems> items = default)
{
	this.Currencies = currencies;
	this.DropTables = dropTables;
	this.Energies = energies;
	this.Items = items;
}

/// Snapser.Model for TrackablesRewardInventoryItems
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesRewardInventoryItems" /> class.
/// </summary>
/// <param name="count">Count of the inventory item to be rewarded.</param>
/// <param name="subInventoryItemName">Sub-inventory item name, if applicable.</param>
public TrackablesRewardInventoryItems(int count = default(int), string subInventoryItemName = default(string))
{
	this.Count = count;
	this.SubInventoryItemName = subInventoryItemName;
}

/// Snapser.Model for TrackablesRewardDropTable
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesRewardDropTable" /> class.
/// </summary>
/// <param name="count">Number of times to evaluate the drop table (default 1).</param>
/// <param name="name">Name of the drop table.</param>
public TrackablesRewardDropTable(int count = default, string name = default)
{
	this.Count = count;
	this.Name = name;
}
```
```cpp
// Request
class SNAPSER_API SnapserTrackablesServiceApi::TrackablesSetUserXpRequest : public Request
{
public:
    virtual ~TrackablesSetUserXpRequest() {}
	void SetupHttpRequest(const FHttpRequestRef& HttpRequest) const final;
	FString ComputePath() const final;

	/* UserID of the affected user */
	FString UserId;
	/* Name of xp to set */
	FString Name;
	/* User session token */
	FString Token;
	SnapserSetUserXpRequest Body;
};

class SNAPSER_API SnapserSetUserXpRequest : public Model
{
public:
    virtual ~SnapserSetUserXpRequest() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Value to be set as the user xp statistic */
	int64 Value = 0;
};

// Response
class SNAPSER_API SnapserTrackablesServiceApi::TrackablesSetUserXpResponse : public Response
{
public:
    virtual ~TrackablesSetUserXpResponse() {}
	void SetHttpResponseCode(EHttpResponseCodes::Type InHttpResponseCode) final;
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;

    SnapserTrackablesUpdateUserXpInfo Content;
};

class SNAPSER_API SnapserTrackablesUpdateUserXpInfo : public Model
{
public:
    virtual ~SnapserTrackablesUpdateUserXpInfo() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Xp of user before the update */
	TOptional<int64> AfterXp;
	/* Xp of user before the update */
	TOptional<int64> BeforeXp;
	TOptional<SnapserTrackablesLevel> CurrentLevel;
	/* Array of reward grant statuses for level completion rewards for each level progressed */
	TOptional<TArray<SnapserTrackablesRewardGrantStatus>> LevelCompletionRewards;
	/* Boolean indicating if the user have levelled up because of update */
	TOptional<bool> LevelUp;
	/* Array of reward grant statuses for level up rewards for each level progressed */
	TOptional<TArray<SnapserTrackablesRewardGrantStatus>> LevelUpRewards;
	/* Array of levels progressed by the user in the event of level up */
	TOptional<TArray<SnapserTrackablesLevel>> LevelsProgressed;
	/* Metadata associated with the xp setting */
	TOptional<TSharedPtr<FJsonObject>> Metadata;
	/* Up to next 3 levels in progression */
	TOptional<TArray<SnapserTrackablesLevel>> NextLevels;
	/* Remaining Xp after final level reached */
	TOptional<int64> OverflowXp;
	/* Array of reward grant statuses for each level progressed (DEPRECATED: Use level_completion_rewards, or level_up_rewards) */
	TOptional<TArray<SnapserTrackablesRewardGrantStatus>> RewardGrantStatuses;
	/* Boolean indicator of rewards granted per level progressed (Deprecated: Use level_completion_rewards, or level_up_rewards) */
	TOptional<TMap<FString, bool>> RewardsGrantStatusPerLevel;
	/* Boolean indicating if the user have been granted rewards successfully for all the levels progressed */
	TOptional<bool> RewardsGranted;
	/* Status of xp update, true if xp updated successfully */
	TOptional<bool> Success;
	/* UserID of the user */
	TOptional<FString> UserId;
};

class SNAPSER_API SnapserTrackablesRewardGrantStatus : public Model
{
public:
    virtual ~SnapserTrackablesRewardGrantStatus() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Results from drop table evaluations */
	TOptional<TArray<SnapserTrackablesDropTableGrantResult>> DropTableResults;
	/* Drop tables that failed to grant */
	TOptional<TArray<FString>> FailedDropTables;
	/* Name of the level for which the reward was granted */
	TOptional<FString> LevelName;
	TOptional<SnapserTrackablesRewardGrantStatusStatus> Status;
	/* Indicates if the reward was granted successfully. Deprecated: Use status field */
	TOptional<bool> Success;
};

class SNAPSER_API SnapserTrackablesDropTableGrantResult : public Model
{
public:
    virtual ~SnapserTrackablesDropTableGrantResult() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Currencies granted from this drop table evaluation */
	TOptional<TMap<FString, FString>> CurrenciesGranted;
	/* Index of the drop that was selected */
	TOptional<int32> DropIndex;
	/* Name of the drop table that was evaluated */
	TOptional<FString> DropTableName;
	/* Energy granted from this drop table evaluation */
	TOptional<TMap<FString, int32>> EnergyGranted;
	/* Items granted from this drop table evaluation */
	TOptional<TMap<FString, int32>> ItemsGranted;
};

class SNAPSER_API SnapserTrackablesRewardGrantStatusStatus : public Model
{
public:
    virtual ~SnapserTrackablesRewardGrantStatusStatus() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	enum class Values
	{
		UnknownStatus,
		Granted,
		Failed,
		NotConfigured,
		Skipped,
  	};

	Values Value;

	static FString EnumToString(const Values& EnumValue);
	static bool EnumFromString(const FString& EnumAsString, Values& EnumValue);
};

class SNAPSER_API SnapserTrackablesLevel : public Model
{
public:
    virtual ~SnapserTrackablesLevel() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Ending xp for the level */
	TOptional<int64> End;
	/* Index of the level in the xp setting */
	TOptional<int32> Index;
	TOptional<SnapserTrackablesRewards> LevelCompletionRewards;
	TOptional<SnapserTrackablesRewards> LevelUpRewards;
	/* Metadata associated with the level */
	TOptional<TSharedPtr<FJsonObject>> Metadata;
	/* Name of the level */
	TOptional<FString> Name;
	/* Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, int32>> RewardCurrencies;
	/* 64-bit Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, FString>> RewardCurrencies64;
	/* Energies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, int32>> RewardEnergies;
	/* Inventory items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, SnapserTrackablesRewardInventoryItems>> RewardInventoryItems;
	/* Items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, int32>> RewardItems;
	/* Starting xp for the level */
	TOptional<int64> Start;
};

class SNAPSER_API SnapserTrackablesRewards : public Model
{
public:
    virtual ~SnapserTrackablesRewards() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Currencies to be rewarded */
	TOptional<TMap<FString, FString>> Currencies;
	/* Drop tables to grant */
	TOptional<TArray<SnapserTrackablesRewardDropTable>> DropTables;
	/* Energies to be rewarded */
	TOptional<TMap<FString, int32>> Energies;
	/* Items to be rewarded */
	TOptional<TMap<FString, SnapserTrackablesRewardInventoryItems>> Items;
};

class SNAPSER_API SnapserTrackablesRewardDropTable : public Model
{
public:
    virtual ~SnapserTrackablesRewardDropTable() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Number of times to evaluate the drop table (default 1) */
	TOptional<int32> Count;
	/* Name of the drop table */
	TOptional<FString> Name;
};

class SNAPSER_API SnapserTrackablesRewardInventoryItems : public Model
{
public:
    virtual ~SnapserTrackablesRewardInventoryItems() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Count of the inventory item to be rewarded */
	TOptional<int32> Count;
	/* Sub-inventory item name, if applicable */
	TOptional<FString> SubInventoryItemName;
};
```
```go
// Coming soon
```
```python
# Coming soon
```
</CodeGroup>
#### TrackablesBatchConsumeUserEnergy API
<CodeGroup title="Sets the xp for a given user" tag="PUT" label="TrackablesBatchConsumeUserEnergy">
```csharp
/// <summary>
/// Consume user energy Consumes the energy of the users
/// </summary>
/// <exception cref="Snapser.Client.ApiException">Thrown when fails to make API call</exception>
/// <param name="token">(app auth only)</param>
/// <param name="body"></param>
/// <returns>TrackablesBatchConsumeUserEnergyResponse</returns>
public TrackablesBatchConsumeUserEnergyResponse TrackablesBatchConsumeUserEnergy(string token, TrackablesBatchConsumeUserEnergyRequest body)
{
    Snapser.Client.ApiResponse<TrackablesBatchConsumeUserEnergyResponse> localVarResponse = TrackablesBatchConsumeUserEnergyWithHttpInfo(token, body);
    return localVarResponse.Data;
}

// Snapser.Model for TrackablesBatchConsumeUserEnergyRequest
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesBatchConsumeUserEnergyRequest" /> class.
/// </summary>
/// <param name="requests">requests.</param>
public TrackablesBatchConsumeUserEnergyRequest(List<TrackablesConsumeUserEnergyRequest> requests = default(List<TrackablesConsumeUserEnergyRequest>))
{
	this.Requests = requests;
}

/// Snapser.Model for TrackablesConsumeUserEnergyRequest
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesConsumeUserEnergyRequest" /> class.
/// </summary>
/// <param name="delta">Value to decrement the energy by (required).</param>
/// <param name="energyName">Name of energy to consume (required).</param>
/// <param name="userId">UserID of the affected user (required).</param>
public TrackablesConsumeUserEnergyRequest(long delta = default(long), string energyName = default(string), string userId = default(string))
{
    this.Delta = delta;
    // to ensure "energyName" is required (not null)
    if (energyName == null)
    {
        throw new ArgumentNullException("energyName is a required property for TrackablesConsumeUserEnergyRequest and cannot be null");
    }
    this.EnergyName = energyName;
    // to ensure "userId" is required (not null)
    if (userId == null)
    {
        throw new ArgumentNullException("userId is a required property for TrackablesConsumeUserEnergyRequest and cannot be null");
    }
    this.UserId = userId;
}

// Snapser.Model for TrackablesBatchConsumeUserEnergyResponse
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesBatchConsumeUserEnergyResponse" /> class.
/// </summary>
/// <param name="responses">responses.</param>
public TrackablesBatchConsumeUserEnergyResponse(List<TrackablesBatchConsumeSingleUserEnergyResponse> responses = default(List<TrackablesBatchConsumeSingleUserEnergyResponse>))
{
	this.Responses = responses;
}

/// Snapser.Model for TrackablesBatchConsumeSingleUserEnergyResponse
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesBatchConsumeSingleUserEnergyResponse" /> class.
/// </summary>
/// <param name="message">message.</param>
/// <param name="request">request.</param>
/// <param name="response">response.</param>
/// <param name="success">success.</param>
public TrackablesBatchConsumeSingleUserEnergyResponse(string message = default(string), TrackablesConsumeUserEnergyRequest request = default(TrackablesConsumeUserEnergyRequest), TrackablesUpdateUserEnergyInfo response = default(TrackablesUpdateUserEnergyInfo), bool success = default(bool))
{
		this.Message = message;
		this.Request = request;
		this.Response = response;
		this.Success = success;
}

/// Snapser.Model for TrackablesConsumeUserEnergyRequest
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesConsumeUserEnergyRequest" /> class.
/// </summary>
/// <param name="delta">Value to decrement the energy by (required).</param>
/// <param name="energyName">Name of energy to consume (required).</param>
/// <param name="userId">UserID of the affected user (required).</param>
public TrackablesConsumeUserEnergyRequest(long delta = default(long), string energyName = default(string), string userId = default(string))
{
		this.Delta = delta;
		// to ensure "energyName" is required (not null)
		if (energyName == null)
		{
				throw new ArgumentNullException("energyName is a required property for TrackablesConsumeUserEnergyRequest and cannot be null");
		}
		this.EnergyName = energyName;
		// to ensure "userId" is required (not null)
		if (userId == null)
		{
				throw new ArgumentNullException("userId is a required property for TrackablesConsumeUserEnergyRequest and cannot be null");
		}
		this.UserId = userId;
}

/// Snapser.Model for TrackablesUpdateUserEnergyInfo
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesUpdateUserEnergyInfo" /> class.
/// </summary>
/// <param name="afterEnergy">Energy of user after the update.</param>
/// <param name="beforeEnergy">Energy of user before the update.</param>
/// <param name="energy">Energy of the user. (DEPRECATED: Use after_energy).</param>
/// <param name="metadata">Metadata associated with the energy setting.</param>
/// <param name="nextEnergyAt">Next energy at Timestamp.</param>
/// <param name="secondsToNextEnergy">Seconds to next energy.</param>
/// <param name="success">Status of energy update, true if energy updated successfully.</param>
/// <param name="userId">UserID of the user.</param>
public TrackablesUpdateUserEnergyInfo(int afterEnergy = default(int), int beforeEnergy = default(int), int energy = default(int), Object metadata = default(Object), long nextEnergyAt = default(long), int secondsToNextEnergy = default(int), bool success = default(bool), string userId = default(string))
{
	this.AfterEnergy = afterEnergy;
	this.BeforeEnergy = beforeEnergy;
	this.Energy = energy;
	this.Metadata = metadata;
	this.NextEnergyAt = nextEnergyAt;
	this.SecondsToNextEnergy = secondsToNextEnergy;
	this.Success = success;
	this.UserId = userId;
}
```
```cpp
// Request
class SNAPSER_API SnapserTrackablesServiceApi::TrackablesBatchConsumeUserEnergyRequest : public Request
{
public:
    virtual ~TrackablesBatchConsumeUserEnergyRequest() {}
	void SetupHttpRequest(const FHttpRequestRef& HttpRequest) const final;
	FString ComputePath() const final;

	/* (app auth only) */
	FString Token;
	SnapserTrackablesBatchConsumeUserEnergyRequest Body;
};

class SNAPSER_API SnapserTrackablesBatchConsumeUserEnergyRequest : public Model
{
public:
    virtual ~SnapserTrackablesBatchConsumeUserEnergyRequest() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	TOptional<TArray<SnapserTrackablesConsumeUserEnergyRequest>> Requests;
};

class SNAPSER_API SnapserTrackablesConsumeUserEnergyRequest : public Model
{
public:
    virtual ~SnapserTrackablesConsumeUserEnergyRequest() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Value to decrement the energy by */
	int64 Delta = 0;
	/* Name of energy to consume */
	FString EnergyName;
	/* UserID of the affected user */
	FString UserId;
};

// Response
class SNAPSER_API SnapserTrackablesServiceApi::TrackablesBatchConsumeUserEnergyResponse : public Response
{
public:
    virtual ~TrackablesBatchConsumeUserEnergyResponse() {}
	void SetHttpResponseCode(EHttpResponseCodes::Type InHttpResponseCode) final;
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;

    SnapserTrackablesBatchConsumeUserEnergyResponse Content;
};

class SNAPSER_API SnapserTrackablesBatchConsumeUserEnergyResponse : public Model
{
public:
    virtual ~SnapserTrackablesBatchConsumeUserEnergyResponse() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	TOptional<TArray<SnapserTrackablesBatchConsumeSingleUserEnergyResponse>> Responses;
};

class SNAPSER_API SnapserTrackablesBatchConsumeSingleUserEnergyResponse : public Model
{
public:
    virtual ~SnapserTrackablesBatchConsumeSingleUserEnergyResponse() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	TOptional<FString> Message;
	TOptional<SnapserTrackablesConsumeUserEnergyRequest> Request;
	TOptional<SnapserTrackablesUpdateUserEnergyInfo> Response;
	TOptional<bool> Success;
};

class SNAPSER_API SnapserTrackablesConsumeUserEnergyRequest : public Model
{
public:
    virtual ~SnapserTrackablesConsumeUserEnergyRequest() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Value to decrement the energy by */
	int64 Delta = 0;
	/* Name of energy to consume */
	FString EnergyName;
	/* UserID of the affected user */
	FString UserId;
};

class SNAPSER_API SnapserTrackablesUpdateUserEnergyInfo : public Model
{
public:
    virtual ~SnapserTrackablesUpdateUserEnergyInfo() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Energy of user after the update */
	TOptional<int32> AfterEnergy;
	/* Energy of user before the update */
	TOptional<int32> BeforeEnergy;
	/* Energy of the user. (DEPRECATED: Use after_energy) */
	TOptional<int32> Energy;
	/* Metadata associated with the energy setting */
	TOptional<TSharedPtr<FJsonObject>> Metadata;
	/* Next energy at Timestamp */
	TOptional<int64> NextEnergyAt;
	/* Seconds to next energy */
	TOptional<int32> SecondsToNextEnergy;
	/* Status of energy update, true if energy updated successfully */
	TOptional<bool> Success;
	/* UserID of the user */
	TOptional<FString> UserId;
};

```
```go
// Coming soon
```
```python
# Coming soon
```
</CodeGroup>
#### TrackablesBatchGetUserEnergy API
<CodeGroup title="Sets the xp for a given user" tag="PUT" label="TrackablesBatchGetUserEnergy">
```csharp
/// <summary>
/// User Energy (App Auth) Gets the energy of the users
/// </summary>
/// <exception cref="Snapser.Client.ApiException">Thrown when fails to make API call</exception>
/// <param name="energyName">Name of energy</param>
/// <param name="userId">User IDs to get energy for</param>
/// <param name="token">(app auth only)</param>
/// <returns>TrackablesBatchGetUserEnergyResponse</returns>
public TrackablesBatchGetUserEnergyResponse TrackablesBatchGetUserEnergy(string energyName, List<string> userId, string token)
{
    Snapser.Client.ApiResponse<TrackablesBatchGetUserEnergyResponse> localVarResponse = TrackablesBatchGetUserEnergyWithHttpInfo(energyName, userId, token);
    return localVarResponse.Data;
}

/// Snapser.Model for TrackablesBatchGetUserEnergyResponse
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesBatchGetUserEnergyResponse" /> class.
/// </summary>
/// <param name="responses">responses.</param>
/// <param name="results">results.</param>
public TrackablesBatchGetUserEnergyResponse(List<TrackablesGetUserEnergySummaryResponse> responses = default(List<TrackablesGetUserEnergySummaryResponse>), List<TrackablesBatchGetUserSingleEnergyResponse> results = default(List<TrackablesBatchGetUserSingleEnergyResponse>))
{
		this.Responses = responses;
		this.Results = results;
}

/// Snapser.Model for TrackablesGetUserEnergySummaryResponse
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesGetUserEnergySummaryResponse" /> class.
/// </summary>
/// <param name="energy">Energy of the user.</param>
/// <param name="energyName">Name of energy.</param>
/// <param name="metadata">Metadata associated with the energy setting.</param>
/// <param name="nextEnergyAt">Next energy at Timestamp.</param>
/// <param name="secondsToNextEnergy">Seconds to next energy.</param>
/// <param name="userId">UserID of the user.</param>
public TrackablesGetUserEnergySummaryResponse(int energy = default(int), string energyName = default(string), Object metadata = default(Object), long nextEnergyAt = default(long), int secondsToNextEnergy = default(int), string userId = default(string))
{
	this.Energy = energy;
	this.EnergyName = energyName;
	this.Metadata = metadata;
	this.NextEnergyAt = nextEnergyAt;
	this.SecondsToNextEnergy = secondsToNextEnergy;
	this.UserId = userId;
}

/// Snapser.Model for TrackablesBatchGetUserSingleEnergyResponse
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesBatchGetUserSingleEnergyResponse" /> class.
/// </summary>
/// <param name="message">message.</param>
/// <param name="request">request.</param>
/// <param name="response">response.</param>
/// <param name="success">success.</param>
public TrackablesBatchGetUserSingleEnergyResponse(string message = default(string), TrackablesGetUserEnergyRequest request = default(TrackablesGetUserEnergyRequest), TrackablesGetUserEnergySummaryResponse response = default(TrackablesGetUserEnergySummaryResponse), bool success = default(bool))
{
		this.Message = message;
		this.Request = request;
		this.Response = response;
		this.Success = success;
}
```
```cpp
// Request
class SNAPSER_API SnapserTrackablesServiceApi::TrackablesBatchGetUserEnergyRequest : public Request
{
public:
    virtual ~TrackablesBatchGetUserEnergyRequest() {}
	void SetupHttpRequest(const FHttpRequestRef& HttpRequest) const final;
	FString ComputePath() const final;

	/* Name of energy */
	FString EnergyName;
	/* User IDs to get energy for */
	TArray<FString> UserId;
	/* (app auth only) */
	FString Token;
};

// Response
class SNAPSER_API SnapserTrackablesServiceApi::TrackablesBatchGetUserEnergyResponse : public Response
{
public:
    virtual ~TrackablesBatchGetUserEnergyResponse() {}
	void SetHttpResponseCode(EHttpResponseCodes::Type InHttpResponseCode) final;
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;

    SnapserTrackablesBatchGetUserEnergyResponse Content;
};

class SNAPSER_API SnapserTrackablesBatchGetUserEnergyResponse : public Model
{
public:
    virtual ~SnapserTrackablesBatchGetUserEnergyResponse() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	TOptional<TArray<SnapserTrackablesGetUserEnergySummaryResponse>> Responses;
	TOptional<TArray<SnapserTrackablesBatchGetUserSingleEnergyResponse>> Results;
};

class SNAPSER_API SnapserTrackablesGetUserEnergySummaryResponse : public Model
{
public:
    virtual ~SnapserTrackablesGetUserEnergySummaryResponse() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Energy of the user */
	TOptional<int32> Energy;
	/* Name of energy */
	TOptional<FString> EnergyName;
	/* Metadata associated with the energy setting */
	TOptional<TSharedPtr<FJsonObject>> Metadata;
	/* Next energy at Timestamp */
	TOptional<int64> NextEnergyAt;
	/* Seconds to next energy */
	TOptional<int32> SecondsToNextEnergy;
	/* UserID of the user */
	TOptional<FString> UserId;
};

class SNAPSER_API SnapserTrackablesBatchGetUserSingleEnergyResponse : public Model
{
public:
    virtual ~SnapserTrackablesBatchGetUserSingleEnergyResponse() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	TOptional<FString> Message;
	TOptional<SnapserTrackablesGetUserEnergyRequest> Request;
	TOptional<SnapserTrackablesGetUserEnergySummaryResponse> Response;
	TOptional<bool> Success;
};
```
```go
// Coming soon
```
```python
# Coming soon
```
</CodeGroup>
#### TrackablesBatchGetUserTrackables API
<CodeGroup title="Sets the xp for a given user" tag="PUT" label="TrackablesBatchGetUserTrackables">
```csharp
/// <summary>
/// User Trackables (App Auth) Gets the trackables (xp and energy) for any number of users
/// </summary>
/// <exception cref="Snapser.Client.ApiException">Thrown when fails to make API call</exception>
/// <param name="userId">User IDs to get trackables for</param>
/// <param name="token">(app auth only)</param>
/// <returns>TrackablesBatchGetUserTrackablesResponse</returns>
public TrackablesBatchGetUserTrackablesResponse TrackablesBatchGetUserTrackables(List<string> userId, string token)
{
	Snapser.Client.ApiResponse<TrackablesBatchGetUserTrackablesResponse> localVarResponse = TrackablesBatchGetUserTrackablesWithHttpInfo(userId, token);
	return localVarResponse.Data;
}

/// Snapser.Model for TrackablesBatchGetUserTrackablesResponse
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesBatchGetUserTrackablesResponse" /> class.
/// </summary>
/// <param name="responses">responses.</param>
public TrackablesBatchGetUserTrackablesResponse(List<TrackablesBatchGetUserSingleTrackablesResponse> responses = default(List<TrackablesBatchGetUserSingleTrackablesResponse>))
{
	this.Responses = responses;
}

/// Snapser.Model for TrackablesBatchGetUserSingleTrackablesResponse
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesBatchGetUserSingleTrackablesResponse" /> class.
/// </summary>
/// <param name="message">message.</param>
/// <param name="response">response.</param>
/// <param name="success">success.</param>
/// <param name="userId">userId.</param>
public TrackablesBatchGetUserSingleTrackablesResponse(string message = default(string), TrackablesGetUserTrackablesResponse response = default(TrackablesGetUserTrackablesResponse), bool success = default(bool), string userId = default(string))
{
	this.Message = message;
	this.Response = response;
	this.Success = success;
	this.UserId = userId;
}

/// Snapser.Model for TrackablesGetUserTrackablesResponse
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesGetUserTrackablesResponse" /> class.
/// </summary>
/// <param name="energy">energy.</param>
/// <param name="xp">xp.</param>
public TrackablesGetUserTrackablesResponse(List<TrackablesGetUserEnergySummaryResponse> energy = default(List<TrackablesGetUserEnergySummaryResponse>), List<TrackablesGetUserXpSummaryResponse> xp = default(List<TrackablesGetUserXpSummaryResponse>))
{
	this.Energy = energy;
	this.Xp = xp;
}

/// Snapser.Model for TrackablesGetUserEnergySummaryResponse
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesGetUserEnergySummaryResponse" /> class.
/// </summary>
/// <param name="energy">Energy of the user.</param>
/// <param name="energyName">Name of energy.</param>
/// <param name="metadata">Metadata associated with the energy setting.</param>
/// <param name="nextEnergyAt">Next energy at Timestamp.</param>
/// <param name="secondsToNextEnergy">Seconds to next energy.</param>
/// <param name="userId">UserID of the user.</param>
public TrackablesGetUserEnergySummaryResponse(int energy = default(int), string energyName = default(string), Object metadata = default(Object), long nextEnergyAt = default(long), int secondsToNextEnergy = default(int), string userId = default(string))
{
	this.Energy = energy;
	this.EnergyName = energyName;
	this.Metadata = metadata;
	this.NextEnergyAt = nextEnergyAt;
	this.SecondsToNextEnergy = secondsToNextEnergy;
	this.UserId = userId;
}

/// Snapser.Model for TrackablesGetUserXpSummaryResponse
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesGetUserXpSummaryResponse" /> class.
/// </summary>
/// <param name="currentLevel">currentLevel.</param>
/// <param name="metadata">Metadata associated with the xp setting.</param>
/// <param name="nextLevels">Up to next 3 levels in progression.</param>
/// <param name="userId">UserID of the user.</param>
/// <param name="xp">Xp of the user.</param>
/// <param name="xpName">Name of xp.</param>
public TrackablesGetUserXpSummaryResponse(TrackablesLevel currentLevel = default(TrackablesLevel), Object metadata = default(Object), List<TrackablesLevel> nextLevels = default(List<TrackablesLevel>), string userId = default(string), long xp = default(long), string xpName = default(string))
{
	this.CurrentLevel = currentLevel;
	this.Metadata = metadata;
	this.NextLevels = nextLevels;
	this.UserId = userId;
	this.Xp = xp;
	this.XpName = xpName;
}

/// Snapser.Model for TrackablesLevel
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesLevel" /> class.
/// </summary>
/// <param name="end">Ending xp for the level.</param>
/// <param name="index">Index of the level in the xp setting.</param>
/// <param name="levelCompletionRewards">levelCompletionRewards.</param>
/// <param name="levelUpRewards">levelUpRewards.</param>
/// <param name="metadata">Metadata associated with the level.</param>
/// <param name="name">Name of the level.</param>
/// <param name="rewardCurrencies">Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardCurrencies64">64-bit Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardEnergies">Energies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardInventoryItems">Inventory items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardItems">Items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="start">Starting xp for the level.</param>
public TrackablesLevel(long end = default(long), int index = default(int), TrackablesRewards levelCompletionRewards = default(TrackablesRewards), TrackablesRewards levelUpRewards = default(TrackablesRewards), Object metadata = default(Object), string name = default(string), Dictionary<string, int> rewardCurrencies = default(Dictionary<string, int>), Dictionary<string, string> rewardCurrencies64 = default(Dictionary<string, string>), Dictionary<string, int> rewardEnergies = default(Dictionary<string, int>), Dictionary<string, TrackablesRewardInventoryItems> rewardInventoryItems = default(Dictionary<string, TrackablesRewardInventoryItems>), Dictionary<string, int> rewardItems = default(Dictionary<string, int>), long start = default(long))
{
	this.End = end;
	this.Index = index;
	this.LevelCompletionRewards = levelCompletionRewards;
	this.LevelUpRewards = levelUpRewards;
	this.Metadata = metadata;
	this.Name = name;
	this.RewardCurrencies = rewardCurrencies;
	this.RewardCurrencies64 = rewardCurrencies64;
	this.RewardEnergies = rewardEnergies;
	this.RewardInventoryItems = rewardInventoryItems;
	this.RewardItems = rewardItems;
	this.Start = start;
}

/// Snapser.Model for TrackablesRewards
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesRewards" /> class.
/// </summary>
/// <param name="currencies">Currencies to be rewarded.</param>
/// <param name="dropTables">Drop tables to grant.</param>
/// <param name="energies">Energies to be rewarded.</param>
/// <param name="items">Items to be rewarded.</param>
public TrackablesRewards(Dictionary<string, string> currencies = default, List<TrackablesRewardDropTable> dropTables = default, Dictionary<string, int> energies = default, Dictionary<string, TrackablesRewardInventoryItems> items = default)
{
	this.Currencies = currencies;
	this.DropTables = dropTables;
	this.Energies = energies;
	this.Items = items;
}

/// Snapser.Model for TrackablesRewardInventoryItems
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesRewardInventoryItems" /> class.
/// </summary>
/// <param name="count">Count of the inventory item to be rewarded.</param>
/// <param name="subInventoryItemName">Sub-inventory item name, if applicable.</param>
public TrackablesRewardInventoryItems(int count = default(int), string subInventoryItemName = default(string))
{
	this.Count = count;
	this.SubInventoryItemName = subInventoryItemName;
}

/// Snapser.Model for TrackablesRewardDropTable
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesRewardDropTable" /> class.
/// </summary>
/// <param name="count">Number of times to evaluate the drop table (default 1).</param>
/// <param name="name">Name of the drop table.</param>
public TrackablesRewardDropTable(int count = default, string name = default)
{
	this.Count = count;
	this.Name = name;
}

```
```cpp
// Request
class SNAPSER_API SnapserTrackablesServiceApi::TrackablesBatchGetUserTrackablesRequest : public Request
{
public:
    virtual ~TrackablesBatchGetUserTrackablesRequest() {}
	void SetupHttpRequest(const FHttpRequestRef& HttpRequest) const final;
	FString ComputePath() const final;

	/* User IDs to get trackables for */
	TArray<FString> UserId;
	/* (app auth only) */
	FString Token;
};

// Response
class SNAPSER_API SnapserTrackablesServiceApi::TrackablesBatchGetUserTrackablesResponse : public Response
{
public:
    virtual ~TrackablesBatchGetUserTrackablesResponse() {}
	void SetHttpResponseCode(EHttpResponseCodes::Type InHttpResponseCode) final;
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;

    SnapserTrackablesBatchGetUserTrackablesResponse Content;
};

class SNAPSER_API SnapserTrackablesBatchGetUserTrackablesResponse : public Model
{
public:
    virtual ~SnapserTrackablesBatchGetUserTrackablesResponse() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	TOptional<TArray<SnapserTrackablesBatchGetUserSingleTrackablesResponse>> Responses;
};

class SNAPSER_API SnapserTrackablesBatchGetUserSingleTrackablesResponse : public Model
{
public:
    virtual ~SnapserTrackablesBatchGetUserSingleTrackablesResponse() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	TOptional<FString> Message;
	TOptional<SnapserTrackablesGetUserTrackablesResponse> Response;
	TOptional<bool> Success;
	TOptional<FString> UserId;
};

class SNAPSER_API SnapserTrackablesGetUserTrackablesResponse : public Model
{
public:
    virtual ~SnapserTrackablesGetUserTrackablesResponse() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	TOptional<TArray<SnapserTrackablesGetUserEnergySummaryResponse>> Energy;
	TOptional<TArray<SnapserTrackablesGetUserXpSummaryResponse>> Xp;
};

class SNAPSER_API SnapserTrackablesGetUserEnergySummaryResponse : public Model
{
public:
    virtual ~SnapserTrackablesGetUserEnergySummaryResponse() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Energy of the user */
	TOptional<int32> Energy;
	/* Name of energy */
	TOptional<FString> EnergyName;
	/* Metadata associated with the energy setting */
	TOptional<TSharedPtr<FJsonObject>> Metadata;
	/* Next energy at Timestamp */
	TOptional<int64> NextEnergyAt;
	/* Seconds to next energy */
	TOptional<int32> SecondsToNextEnergy;
	/* UserID of the user */
	TOptional<FString> UserId;
};

class SNAPSER_API SnapserTrackablesGetUserXpSummaryResponse : public Model
{
public:
    virtual ~SnapserTrackablesGetUserXpSummaryResponse() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	TOptional<SnapserTrackablesLevel> CurrentLevel;
	/* Metadata associated with the xp setting */
	TOptional<TSharedPtr<FJsonObject>> Metadata;
	/* Up to next 3 levels in progression */
	TOptional<TArray<SnapserTrackablesLevel>> NextLevels;
	/* UserID of the user */
	TOptional<FString> UserId;
	/* Xp of the user */
	TOptional<int64> Xp;
	/* Name of xp */
	TOptional<FString> XpName;
};

class SNAPSER_API SnapserTrackablesLevel : public Model
{
public:
    virtual ~SnapserTrackablesLevel() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Ending xp for the level */
	TOptional<int64> End;
	/* Index of the level in the xp setting */
	TOptional<int32> Index;
	TOptional<SnapserTrackablesRewards> LevelCompletionRewards;
	TOptional<SnapserTrackablesRewards> LevelUpRewards;
	/* Metadata associated with the level */
	TOptional<TSharedPtr<FJsonObject>> Metadata;
	/* Name of the level */
	TOptional<FString> Name;
	/* Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, int32>> RewardCurrencies;
	/* 64-bit Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, FString>> RewardCurrencies64;
	/* Energies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, int32>> RewardEnergies;
	/* Inventory items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, SnapserTrackablesRewardInventoryItems>> RewardInventoryItems;
	/* Items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, int32>> RewardItems;
	/* Starting xp for the level */
	TOptional<int64> Start;
};

class SNAPSER_API SnapserTrackablesRewards : public Model
{
public:
    virtual ~SnapserTrackablesRewards() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Currencies to be rewarded */
	TOptional<TMap<FString, FString>> Currencies;
	/* Drop tables to grant */
	TOptional<TArray<SnapserTrackablesRewardDropTable>> DropTables;
	/* Energies to be rewarded */
	TOptional<TMap<FString, int32>> Energies;
	/* Items to be rewarded */
	TOptional<TMap<FString, SnapserTrackablesRewardInventoryItems>> Items;
};

class SNAPSER_API SnapserTrackablesRewardDropTable : public Model
{
public:
    virtual ~SnapserTrackablesRewardDropTable() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Number of times to evaluate the drop table (default 1) */
	TOptional<int32> Count;
	/* Name of the drop table */
	TOptional<FString> Name;
};

class SNAPSER_API SnapserTrackablesRewardInventoryItems : public Model
{
public:
    virtual ~SnapserTrackablesRewardInventoryItems() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Count of the inventory item to be rewarded */
	TOptional<int32> Count;
	/* Sub-inventory item name, if applicable */
	TOptional<FString> SubInventoryItemName;
};

```
```go
// Coming soon
```
```python
# Coming soon
```
</CodeGroup>
#### TrackablesBatchGetUserXp API
<CodeGroup title="Sets the xp for a given user" tag="PUT" label="TrackablesBatchGetUserXp">
```csharp
/// <summary>
/// User XP (App Auth) Gets the xp of given users
/// </summary>
/// <exception cref="Snapser.Client.ApiException">Thrown when fails to make API call</exception>
/// <param name="xpName">Name of xp</param>
/// <param name="userId">User IDs to get xp for</param>
/// <param name="token">(app auth only)</param>
/// <returns>TrackablesBatchGetUserXpResponse</returns>
public TrackablesBatchGetUserXpResponse TrackablesBatchGetUserXp(string xpName, List<string> userId, string token)
{
    Snapser.Client.ApiResponse<TrackablesBatchGetUserXpResponse> localVarResponse = TrackablesBatchGetUserXpWithHttpInfo(xpName, userId, token);
    return localVarResponse.Data;
}

/// Snapser.Model for TrackablesBatchGetUserXpResponse
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesBatchGetUserXpResponse" /> class.
/// </summary>
/// <param name="responses">responses.</param>
/// <param name="results">results.</param>
public TrackablesBatchGetUserXpResponse(List<TrackablesGetUserXpSummaryResponse> responses = default(List<TrackablesGetUserXpSummaryResponse>), List<TrackablesBatchGetUserXpSingleResponse> results = default(List<TrackablesBatchGetUserXpSingleResponse>))
{
    this.Responses = responses;
    this.Results = results;
}

/// Snapser.Model for TrackablesGetUserXpSummaryResponse
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesGetUserXpSummaryResponse" /> class.
/// </summary>
/// <param name="currentLevel">currentLevel.</param>
/// <param name="metadata">Metadata associated with the xp setting.</param>
/// <param name="nextLevels">Up to next 3 levels in progression.</param>
/// <param name="userId">UserID of the user.</param>
/// <param name="xp">Xp of the user.</param>
/// <param name="xpName">Name of xp.</param>
public TrackablesGetUserXpSummaryResponse(TrackablesLevel currentLevel = default(TrackablesLevel), Object metadata = default(Object), List<TrackablesLevel> nextLevels = default(List<TrackablesLevel>), string userId = default(string), long xp = default(long), string xpName = default(string))
{
	this.CurrentLevel = currentLevel;
	this.Metadata = metadata;
	this.NextLevels = nextLevels;
	this.UserId = userId;
	this.Xp = xp;
	this.XpName = xpName;
}

/// Snapser.Model for TrackablesLevel
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesLevel" /> class.
/// </summary>
/// <param name="end">Ending xp for the level.</param>
/// <param name="index">Index of the level in the xp setting.</param>
/// <param name="levelCompletionRewards">levelCompletionRewards.</param>
/// <param name="levelUpRewards">levelUpRewards.</param>
/// <param name="metadata">Metadata associated with the level.</param>
/// <param name="name">Name of the level.</param>
/// <param name="rewardCurrencies">Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardCurrencies64">64-bit Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardEnergies">Energies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardInventoryItems">Inventory items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardItems">Items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="start">Starting xp for the level.</param>
public TrackablesLevel(long end = default(long), int index = default(int), TrackablesRewards levelCompletionRewards = default(TrackablesRewards), TrackablesRewards levelUpRewards = default(TrackablesRewards), Object metadata = default(Object), string name = default(string), Dictionary<string, int> rewardCurrencies = default(Dictionary<string, int>), Dictionary<string, string> rewardCurrencies64 = default(Dictionary<string, string>), Dictionary<string, int> rewardEnergies = default(Dictionary<string, int>), Dictionary<string, TrackablesRewardInventoryItems> rewardInventoryItems = default(Dictionary<string, TrackablesRewardInventoryItems>), Dictionary<string, int> rewardItems = default(Dictionary<string, int>), long start = default(long))
{
	this.End = end;
	this.Index = index;
	this.LevelCompletionRewards = levelCompletionRewards;
	this.LevelUpRewards = levelUpRewards;
	this.Metadata = metadata;
	this.Name = name;
	this.RewardCurrencies = rewardCurrencies;
	this.RewardCurrencies64 = rewardCurrencies64;
	this.RewardEnergies = rewardEnergies;
	this.RewardInventoryItems = rewardInventoryItems;
	this.RewardItems = rewardItems;
	this.Start = start;
}

/// Snapser.Model for TrackablesRewards
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesRewards" /> class.
/// </summary>
/// <param name="currencies">Currencies to be rewarded.</param>
/// <param name="dropTables">Drop tables to grant.</param>
/// <param name="energies">Energies to be rewarded.</param>
/// <param name="items">Items to be rewarded.</param>
public TrackablesRewards(Dictionary<string, string> currencies = default, List<TrackablesRewardDropTable> dropTables = default, Dictionary<string, int> energies = default, Dictionary<string, TrackablesRewardInventoryItems> items = default)
{
	this.Currencies = currencies;
	this.DropTables = dropTables;
	this.Energies = energies;
	this.Items = items;
}

/// Snapser.Model for TrackablesRewardInventoryItems
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesRewardInventoryItems" /> class.
/// </summary>
/// <param name="count">Count of the inventory item to be rewarded.</param>
/// <param name="subInventoryItemName">Sub-inventory item name, if applicable.</param>
public TrackablesRewardInventoryItems(int count = default(int), string subInventoryItemName = default(string))
{
	this.Count = count;
	this.SubInventoryItemName = subInventoryItemName;
}

/// Snapser.Model for TrackablesRewardDropTable
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesRewardDropTable" /> class.
/// </summary>
/// <param name="count">Number of times to evaluate the drop table (default 1).</param>
/// <param name="name">Name of the drop table.</param>
public TrackablesRewardDropTable(int count = default, string name = default)
{
	this.Count = count;
	this.Name = name;
}

/// Snapser.Model for TrackablesBatchGetUserXpSingleResponse
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesBatchGetUserXpSingleResponse" /> class.
/// </summary>
/// <param name="message">message.</param>
/// <param name="request">request.</param>
/// <param name="response">response.</param>
/// <param name="success">success.</param>
public TrackablesBatchGetUserXpSingleResponse(string message = default(string), TrackablesGetUserXpRequest request = default(TrackablesGetUserXpRequest), TrackablesGetUserXpSummaryResponse response = default(TrackablesGetUserXpSummaryResponse), bool success = default(bool))
{
    this.Message = message;
    this.Request = request;
    this.Response = response;
    this.Success = success;
}

/// Snapser.Model for TrackablesGetUserXpRequest
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesGetUserXpRequest" /> class.
/// </summary>
/// <param name="name">Name of xp (required).</param>
/// <param name="userId">UserID of user (required).</param>
public TrackablesGetUserXpRequest(string name = default(string), string userId = default(string))
{
	// to ensure "name" is required (not null)
	if (name == null)
	{
		throw new ArgumentNullException("name is a required property for TrackablesGetUserXpRequest and cannot be null");
	}
	this.Name = name;
	// to ensure "userId" is required (not null)
	if (userId == null)
	{
		throw new ArgumentNullException("userId is a required property for TrackablesGetUserXpRequest and cannot be null");
	}
	this.UserId = userId;
}

```
```cpp
// Request
class SNAPSER_API SnapserTrackablesServiceApi::TrackablesBatchGetUserXpRequest : public Request
{
public:
    virtual ~TrackablesBatchGetUserXpRequest() {}
	void SetupHttpRequest(const FHttpRequestRef& HttpRequest) const final;
	FString ComputePath() const final;

	/* Name of xp */
	FString XpName;
	/* User IDs to get xp for */
	TArray<FString> UserId;
	/* (app auth only) */
	FString Token;
};

// Response
class SNAPSER_API SnapserTrackablesServiceApi::TrackablesBatchGetUserXpResponse : public Response
{
public:
    virtual ~TrackablesBatchGetUserXpResponse() {}
	void SetHttpResponseCode(EHttpResponseCodes::Type InHttpResponseCode) final;
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;

    SnapserTrackablesBatchGetUserXpResponse Content;
};

class SNAPSER_API SnapserTrackablesBatchGetUserXpResponse : public Model
{
public:
    virtual ~SnapserTrackablesBatchGetUserXpResponse() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	TOptional<TArray<SnapserTrackablesGetUserXpSummaryResponse>> Responses;
	TOptional<TArray<SnapserTrackablesBatchGetUserXpSingleResponse>> Results;
};

class SNAPSER_API SnapserTrackablesGetUserXpSummaryResponse : public Model
{
public:
    virtual ~SnapserTrackablesGetUserXpSummaryResponse() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	TOptional<SnapserTrackablesLevel> CurrentLevel;
	/* Metadata associated with the xp setting */
	TOptional<TSharedPtr<FJsonObject>> Metadata;
	/* Up to next 3 levels in progression */
	TOptional<TArray<SnapserTrackablesLevel>> NextLevels;
	/* UserID of the user */
	TOptional<FString> UserId;
	/* Xp of the user */
	TOptional<int64> Xp;
	/* Name of xp */
	TOptional<FString> XpName;
};

class SNAPSER_API SnapserTrackablesLevel : public Model
{
public:
    virtual ~SnapserTrackablesLevel() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Ending xp for the level */
	TOptional<int64> End;
	/* Index of the level in the xp setting */
	TOptional<int32> Index;
	TOptional<SnapserTrackablesRewards> LevelCompletionRewards;
	TOptional<SnapserTrackablesRewards> LevelUpRewards;
	/* Metadata associated with the level */
	TOptional<TSharedPtr<FJsonObject>> Metadata;
	/* Name of the level */
	TOptional<FString> Name;
	/* Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, int32>> RewardCurrencies;
	/* 64-bit Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, FString>> RewardCurrencies64;
	/* Energies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, int32>> RewardEnergies;
	/* Inventory items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, SnapserTrackablesRewardInventoryItems>> RewardInventoryItems;
	/* Items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, int32>> RewardItems;
	/* Starting xp for the level */
	TOptional<int64> Start;
};

class SNAPSER_API SnapserTrackablesRewards : public Model
{
public:
    virtual ~SnapserTrackablesRewards() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Currencies to be rewarded */
	TOptional<TMap<FString, FString>> Currencies;
	/* Drop tables to grant */
	TOptional<TArray<SnapserTrackablesRewardDropTable>> DropTables;
	/* Energies to be rewarded */
	TOptional<TMap<FString, int32>> Energies;
	/* Items to be rewarded */
	TOptional<TMap<FString, SnapserTrackablesRewardInventoryItems>> Items;
};

class SNAPSER_API SnapserTrackablesRewardDropTable : public Model
{
public:
    virtual ~SnapserTrackablesRewardDropTable() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Number of times to evaluate the drop table (default 1) */
	TOptional<int32> Count;
	/* Name of the drop table */
	TOptional<FString> Name;
};

class SNAPSER_API SnapserTrackablesRewardInventoryItems : public Model
{
public:
    virtual ~SnapserTrackablesRewardInventoryItems() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Count of the inventory item to be rewarded */
	TOptional<int32> Count;
	/* Sub-inventory item name, if applicable */
	TOptional<FString> SubInventoryItemName;
};

class SNAPSER_API SnapserTrackablesBatchGetUserXpSingleResponse : public Model
{
public:
    virtual ~SnapserTrackablesBatchGetUserXpSingleResponse() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	TOptional<FString> Message;
	TOptional<SnapserTrackablesGetUserXpRequest> Request;
	TOptional<SnapserTrackablesGetUserXpSummaryResponse> Response;
	TOptional<bool> Success;
};

class SNAPSER_API SnapserTrackablesGetUserXpRequest : public Model
{
public:
    virtual ~SnapserTrackablesGetUserXpRequest() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Name of xp */
	FString Name;
	/* UserID of user */
	FString UserId;
};

```
```go
// Coming soon
```
```python
# Coming soon
```
</CodeGroup>
#### TrackablesBatchIncrementUserXp API
<CodeGroup title="Sets the xp for a given user" tag="PUT" label="TrackablesBatchIncrementUserXp">
```csharp
/// <summary>
/// User XP (App Auth) Increments the xp of given users
/// </summary>
/// <exception cref="Snapser.Client.ApiException">Thrown when fails to make API call</exception>
/// <param name="token">(app auth only)</param>
/// <param name="body"></param>
/// <returns>TrackablesBatchIncrementUserXpResponse</returns>
public TrackablesBatchIncrementUserXpResponse TrackablesBatchIncrementUserXp(string token, TrackablesBatchIncrementUserXpRequest body)
{
    Snapser.Client.ApiResponse<TrackablesBatchIncrementUserXpResponse> localVarResponse = TrackablesBatchIncrementUserXpWithHttpInfo(token, body);
    return localVarResponse.Data;
}

/// Snapser.Model for TrackablesBatchIncrementUserXpRequest
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesBatchIncrementUserXpRequest" /> class.
/// </summary>
/// <param name="requests">requests.</param>
public TrackablesBatchIncrementUserXpRequest(List<TrackablesIncrementUserXpRequest> requests = default(List<TrackablesIncrementUserXpRequest>))
{
    this.Requests = requests;
}

/// Snapser.Model for TrackablesIncrementUserXpRequest
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesIncrementUserXpRequest" /> class.
/// </summary>
/// <param name="delta">Value to increment the statistic xp by. Should be non negative integer (required).</param>
/// <param name="name">Name of xp to increment (required).</param>
/// <param name="userId">UserID of the affected user (required).</param>
public TrackablesIncrementUserXpRequest(long delta = default(long), string name = default(string), string userId = default(string))
{
	this.Delta = delta;
	// to ensure "name" is required (not null)
	if (name == null)
	{
		throw new ArgumentNullException("name is a required property for TrackablesIncrementUserXpRequest and cannot be null");
	}
	this.Name = name;
	// to ensure "userId" is required (not null)
	if (userId == null)
	{
		throw new ArgumentNullException("userId is a required property for TrackablesIncrementUserXpRequest and cannot be null");
	}
	this.UserId = userId;
}

/// Snapser.Model for TrackablesBatchIncrementUserXpResponse
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesBatchIncrementUserXpResponse" /> class.
/// </summary>
/// <param name="responses">responses.</param>
public TrackablesBatchIncrementUserXpResponse(List<TrackablesBatchIncrementSingleUserXpResponse> responses = default(List<TrackablesBatchIncrementSingleUserXpResponse>))
{
	this.Responses = responses;
}

/// Snapser.Model for TrackablesBatchIncrementSingleUserXpResponse
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesBatchIncrementSingleUserXpResponse" /> class.
/// </summary>
/// <param name="message">message.</param>
/// <param name="request">request.</param>
/// <param name="response">response.</param>
/// <param name="success">success.</param>
public TrackablesBatchIncrementSingleUserXpResponse(string message = default(string), TrackablesIncrementUserXpRequest request = default(TrackablesIncrementUserXpRequest), TrackablesUpdateUserXpInfo response = default(TrackablesUpdateUserXpInfo), bool success = default(bool))
{
	this.Message = message;
	this.Request = request;
	this.Response = response;
	this.Success = success;
}

/// Snapser.Model for TrackablesUpdateUserXpInfo
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesUpdateUserXpInfo" /> class.
/// </summary>
/// <param name="afterXp">Xp of user before the update.</param>
/// <param name="beforeXp">Xp of user before the update.</param>
/// <param name="currentLevel">currentLevel.</param>
/// <param name="levelCompletionRewards">Array of reward grant statuses for level completion rewards for each level progressed.</param>
/// <param name="levelUp">Boolean indicating if the user have levelled up because of update.</param>
/// <param name="levelUpRewards">Array of reward grant statuses for level up rewards for each level progressed.</param>
/// <param name="levelsProgressed">Array of levels progressed by the user in the event of level up.</param>
/// <param name="metadata">Metadata associated with the xp setting.</param>
/// <param name="nextLevels">Up to next 3 levels in progression.</param>
/// <param name="overflowXp">Remaining Xp after final level reached.</param>
/// <param name="rewardGrantStatuses">Array of reward grant statuses for each level progressed (DEPRECATED: Use level_completion_rewards, or level_up_rewards).</param>
/// <param name="rewardsGrantStatusPerLevel">Boolean indicator of rewards granted per level progressed (Deprecated: Use level_completion_rewards, or level_up_rewards).</param>
/// <param name="rewardsGranted">Boolean indicating if the user have been granted rewards successfully for all the levels progressed.</param>
/// <param name="success">Status of xp update, true if xp updated successfully.</param>
/// <param name="userId">UserID of the user.</param>
public TrackablesUpdateUserXpInfo(long afterXp = default(long), long beforeXp = default(long), TrackablesLevel currentLevel = default(TrackablesLevel), List<TrackablesRewardGrantStatus> levelCompletionRewards = default(List<TrackablesRewardGrantStatus>), bool levelUp = default(bool), List<TrackablesRewardGrantStatus> levelUpRewards = default(List<TrackablesRewardGrantStatus>), List<TrackablesLevel> levelsProgressed = default(List<TrackablesLevel>), Object metadata = default(Object), List<TrackablesLevel> nextLevels = default(List<TrackablesLevel>), long overflowXp = default(long), List<TrackablesRewardGrantStatus> rewardGrantStatuses = default(List<TrackablesRewardGrantStatus>), Dictionary<string, bool> rewardsGrantStatusPerLevel = default(Dictionary<string, bool>), bool rewardsGranted = default(bool), bool success = default(bool), string userId = default(string))
{
	this.AfterXp = afterXp;
	this.BeforeXp = beforeXp;
	this.CurrentLevel = currentLevel;
	this.LevelCompletionRewards = levelCompletionRewards;
	this.LevelUp = levelUp;
	this.LevelUpRewards = levelUpRewards;
	this.LevelsProgressed = levelsProgressed;
	this.Metadata = metadata;
	this.NextLevels = nextLevels;
	this.OverflowXp = overflowXp;
	this.RewardGrantStatuses = rewardGrantStatuses;
	this.RewardsGrantStatusPerLevel = rewardsGrantStatusPerLevel;
	this.RewardsGranted = rewardsGranted;
	this.Success = success;
	this.UserId = userId;
}

/// Snapser.Model for TrackablesRewardGrantStatus
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesRewardGrantStatus" /> class.
/// </summary>
/// <param name="dropTableResults">Results from drop table evaluations.</param>
/// <param name="failedDropTables">Drop tables that failed to grant.</param>
/// <param name="levelName">Name of the level for which the reward was granted.</param>
/// <param name="status">status.</param>
/// <param name="success">Indicates if the reward was granted successfully. Deprecated: Use status field.</param>
public TrackablesRewardGrantStatus(List<TrackablesDropTableGrantResult> dropTableResults = default, List<string> failedDropTables = default, string levelName = default, TrackablesRewardGrantStatusStatus? status = default, bool success = default)
{
	this.DropTableResults = dropTableResults;
	this.FailedDropTables = failedDropTables;
	this.LevelName = levelName;
	this.Status = status;
	this.Success = success;
}

/// Snapser.Model for TrackablesDropTableGrantResult
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesDropTableGrantResult" /> class.
/// </summary>
/// <param name="currenciesGranted">Currencies granted from this drop table evaluation.</param>
/// <param name="dropIndex">Index of the drop that was selected.</param>
/// <param name="dropTableName">Name of the drop table that was evaluated.</param>
/// <param name="energyGranted">Energy granted from this drop table evaluation.</param>
/// <param name="itemsGranted">Items granted from this drop table evaluation.</param>
public TrackablesDropTableGrantResult(Dictionary<string, string> currenciesGranted = default, int dropIndex = default, string dropTableName = default, Dictionary<string, int> energyGranted = default, Dictionary<string, int> itemsGranted = default)
{
	this.CurrenciesGranted = currenciesGranted;
	this.DropIndex = dropIndex;
	this.DropTableName = dropTableName;
	this.EnergyGranted = energyGranted;
	this.ItemsGranted = itemsGranted;
}

/// Snapser.Model for TrackablesLevel
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesLevel" /> class.
/// </summary>
/// <param name="end">Ending xp for the level.</param>
/// <param name="index">Index of the level in the xp setting.</param>
/// <param name="levelCompletionRewards">levelCompletionRewards.</param>
/// <param name="levelUpRewards">levelUpRewards.</param>
/// <param name="metadata">Metadata associated with the level.</param>
/// <param name="name">Name of the level.</param>
/// <param name="rewardCurrencies">Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardCurrencies64">64-bit Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardEnergies">Energies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardInventoryItems">Inventory items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardItems">Items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="start">Starting xp for the level.</param>
public TrackablesLevel(long end = default(long), int index = default(int), TrackablesRewards levelCompletionRewards = default(TrackablesRewards), TrackablesRewards levelUpRewards = default(TrackablesRewards), Object metadata = default(Object), string name = default(string), Dictionary<string, int> rewardCurrencies = default(Dictionary<string, int>), Dictionary<string, string> rewardCurrencies64 = default(Dictionary<string, string>), Dictionary<string, int> rewardEnergies = default(Dictionary<string, int>), Dictionary<string, TrackablesRewardInventoryItems> rewardInventoryItems = default(Dictionary<string, TrackablesRewardInventoryItems>), Dictionary<string, int> rewardItems = default(Dictionary<string, int>), long start = default(long))
{
	this.End = end;
	this.Index = index;
	this.LevelCompletionRewards = levelCompletionRewards;
	this.LevelUpRewards = levelUpRewards;
	this.Metadata = metadata;
	this.Name = name;
	this.RewardCurrencies = rewardCurrencies;
	this.RewardCurrencies64 = rewardCurrencies64;
	this.RewardEnergies = rewardEnergies;
	this.RewardInventoryItems = rewardInventoryItems;
	this.RewardItems = rewardItems;
	this.Start = start;
}

/// Snapser.Model for TrackablesRewards
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesRewards" /> class.
/// </summary>
/// <param name="currencies">Currencies to be rewarded.</param>
/// <param name="dropTables">Drop tables to grant.</param>
/// <param name="energies">Energies to be rewarded.</param>
/// <param name="items">Items to be rewarded.</param>
public TrackablesRewards(Dictionary<string, string> currencies = default, List<TrackablesRewardDropTable> dropTables = default, Dictionary<string, int> energies = default, Dictionary<string, TrackablesRewardInventoryItems> items = default)
{
	this.Currencies = currencies;
	this.DropTables = dropTables;
	this.Energies = energies;
	this.Items = items;
}

/// Snapser.Model for TrackablesRewardInventoryItems
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesRewardInventoryItems" /> class.
/// </summary>
/// <param name="count">Count of the inventory item to be rewarded.</param>
/// <param name="subInventoryItemName">Sub-inventory item name, if applicable.</param>
public TrackablesRewardInventoryItems(int count = default(int), string subInventoryItemName = default(string))
{
	this.Count = count;
	this.SubInventoryItemName = subInventoryItemName;
}

/// Snapser.Model for TrackablesRewardDropTable
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesRewardDropTable" /> class.
/// </summary>
/// <param name="count">Number of times to evaluate the drop table (default 1).</param>
/// <param name="name">Name of the drop table.</param>
public TrackablesRewardDropTable(int count = default, string name = default)
{
	this.Count = count;
	this.Name = name;
}

```
```cpp
// Request
class SNAPSER_API SnapserTrackablesServiceApi::TrackablesBatchIncrementUserXpRequest : public Request
{
public:
    virtual ~TrackablesBatchIncrementUserXpRequest() {}
	void SetupHttpRequest(const FHttpRequestRef& HttpRequest) const final;
	FString ComputePath() const final;

	/* (app auth only) */
	FString Token;
	SnapserTrackablesBatchIncrementUserXpRequest Body;
};

class SNAPSER_API SnapserTrackablesBatchIncrementUserXpRequest : public Model
{
public:
    virtual ~SnapserTrackablesBatchIncrementUserXpRequest() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	TOptional<TArray<SnapserTrackablesIncrementUserXpRequest>> Requests;
};

class SNAPSER_API SnapserTrackablesIncrementUserXpRequest : public Model
{
public:
    virtual ~SnapserTrackablesIncrementUserXpRequest() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Value to increment the statistic xp by. Should be non negative integer */
	int64 Delta = 0;
	/* Name of xp to increment */
	FString Name;
	/* UserID of the affected user */
	FString UserId;
};

// Response
class SNAPSER_API SnapserTrackablesServiceApi::TrackablesBatchIncrementUserXpResponse : public Response
{
public:
    virtual ~TrackablesBatchIncrementUserXpResponse() {}
	void SetHttpResponseCode(EHttpResponseCodes::Type InHttpResponseCode) final;
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;

    SnapserTrackablesBatchIncrementUserXpResponse Content;
};

class SNAPSER_API SnapserTrackablesBatchIncrementUserXpResponse : public Model
{
public:
    virtual ~SnapserTrackablesBatchIncrementUserXpResponse() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	TOptional<TArray<SnapserTrackablesBatchIncrementSingleUserXpResponse>> Responses;
};

class SNAPSER_API SnapserTrackablesBatchIncrementSingleUserXpResponse : public Model
{
public:
    virtual ~SnapserTrackablesBatchIncrementSingleUserXpResponse() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	TOptional<FString> Message;
	TOptional<SnapserTrackablesIncrementUserXpRequest> Request;
	TOptional<SnapserTrackablesUpdateUserXpInfo> Response;
	TOptional<bool> Success;
};

class SNAPSER_API SnapserTrackablesUpdateUserXpInfo : public Model
{
public:
    virtual ~SnapserTrackablesUpdateUserXpInfo() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Xp of user before the update */
	TOptional<int64> AfterXp;
	/* Xp of user before the update */
	TOptional<int64> BeforeXp;
	TOptional<SnapserTrackablesLevel> CurrentLevel;
	/* Array of reward grant statuses for level completion rewards for each level progressed */
	TOptional<TArray<SnapserTrackablesRewardGrantStatus>> LevelCompletionRewards;
	/* Boolean indicating if the user have levelled up because of update */
	TOptional<bool> LevelUp;
	/* Array of reward grant statuses for level up rewards for each level progressed */
	TOptional<TArray<SnapserTrackablesRewardGrantStatus>> LevelUpRewards;
	/* Array of levels progressed by the user in the event of level up */
	TOptional<TArray<SnapserTrackablesLevel>> LevelsProgressed;
	/* Metadata associated with the xp setting */
	TOptional<TSharedPtr<FJsonObject>> Metadata;
	/* Up to next 3 levels in progression */
	TOptional<TArray<SnapserTrackablesLevel>> NextLevels;
	/* Remaining Xp after final level reached */
	TOptional<int64> OverflowXp;
	/* Array of reward grant statuses for each level progressed (DEPRECATED: Use level_completion_rewards, or level_up_rewards) */
	TOptional<TArray<SnapserTrackablesRewardGrantStatus>> RewardGrantStatuses;
	/* Boolean indicator of rewards granted per level progressed (Deprecated: Use level_completion_rewards, or level_up_rewards) */
	TOptional<TMap<FString, bool>> RewardsGrantStatusPerLevel;
	/* Boolean indicating if the user have been granted rewards successfully for all the levels progressed */
	TOptional<bool> RewardsGranted;
	/* Status of xp update, true if xp updated successfully */
	TOptional<bool> Success;
	/* UserID of the user */
	TOptional<FString> UserId;
};

class SNAPSER_API SnapserTrackablesRewardGrantStatus : public Model
{
public:
    virtual ~SnapserTrackablesRewardGrantStatus() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Results from drop table evaluations */
	TOptional<TArray<SnapserTrackablesDropTableGrantResult>> DropTableResults;
	/* Drop tables that failed to grant */
	TOptional<TArray<FString>> FailedDropTables;
	/* Name of the level for which the reward was granted */
	TOptional<FString> LevelName;
	TOptional<SnapserTrackablesRewardGrantStatusStatus> Status;
	/* Indicates if the reward was granted successfully. Deprecated: Use status field */
	TOptional<bool> Success;
};

class SNAPSER_API SnapserTrackablesDropTableGrantResult : public Model
{
public:
    virtual ~SnapserTrackablesDropTableGrantResult() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Currencies granted from this drop table evaluation */
	TOptional<TMap<FString, FString>> CurrenciesGranted;
	/* Index of the drop that was selected */
	TOptional<int32> DropIndex;
	/* Name of the drop table that was evaluated */
	TOptional<FString> DropTableName;
	/* Energy granted from this drop table evaluation */
	TOptional<TMap<FString, int32>> EnergyGranted;
	/* Items granted from this drop table evaluation */
	TOptional<TMap<FString, int32>> ItemsGranted;
};

class SNAPSER_API SnapserTrackablesRewardGrantStatusStatus : public Model
{
public:
    virtual ~SnapserTrackablesRewardGrantStatusStatus() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	enum class Values
	{
		UnknownStatus,
		Granted,
		Failed,
		NotConfigured,
		Skipped,
  	};

	Values Value;

	static FString EnumToString(const Values& EnumValue);
	static bool EnumFromString(const FString& EnumAsString, Values& EnumValue);
};

class SNAPSER_API SnapserTrackablesLevel : public Model
{
public:
    virtual ~SnapserTrackablesLevel() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Ending xp for the level */
	TOptional<int64> End;
	/* Index of the level in the xp setting */
	TOptional<int32> Index;
	TOptional<SnapserTrackablesRewards> LevelCompletionRewards;
	TOptional<SnapserTrackablesRewards> LevelUpRewards;
	/* Metadata associated with the level */
	TOptional<TSharedPtr<FJsonObject>> Metadata;
	/* Name of the level */
	TOptional<FString> Name;
	/* Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, int32>> RewardCurrencies;
	/* 64-bit Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, FString>> RewardCurrencies64;
	/* Energies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, int32>> RewardEnergies;
	/* Inventory items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, SnapserTrackablesRewardInventoryItems>> RewardInventoryItems;
	/* Items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, int32>> RewardItems;
	/* Starting xp for the level */
	TOptional<int64> Start;
};

class SNAPSER_API SnapserTrackablesRewards : public Model
{
public:
    virtual ~SnapserTrackablesRewards() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Currencies to be rewarded */
	TOptional<TMap<FString, FString>> Currencies;
	/* Drop tables to grant */
	TOptional<TArray<SnapserTrackablesRewardDropTable>> DropTables;
	/* Energies to be rewarded */
	TOptional<TMap<FString, int32>> Energies;
	/* Items to be rewarded */
	TOptional<TMap<FString, SnapserTrackablesRewardInventoryItems>> Items;
};

class SNAPSER_API SnapserTrackablesRewardDropTable : public Model
{
public:
    virtual ~SnapserTrackablesRewardDropTable() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Number of times to evaluate the drop table (default 1) */
	TOptional<int32> Count;
	/* Name of the drop table */
	TOptional<FString> Name;
};

class SNAPSER_API SnapserTrackablesRewardInventoryItems : public Model
{
public:
    virtual ~SnapserTrackablesRewardInventoryItems() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Count of the inventory item to be rewarded */
	TOptional<int32> Count;
	/* Sub-inventory item name, if applicable */
	TOptional<FString> SubInventoryItemName;
};

```
```go
// Coming soon
```
```python
# Coming soon
```
</CodeGroup>
#### TrackablesBatchSetUserXp API
<CodeGroup title="Sets the xp for a given user" tag="PUT" label="TrackablesBatchSetUserXp">
```csharp
/// <summary>
/// User XP (App Auth) Sets the xp of given users
/// </summary>
/// <exception cref="Snapser.Client.ApiException">Thrown when fails to make API call</exception>
/// <param name="token">(app auth only)</param>
/// <param name="body"></param>
/// <returns>TrackablesBatchSetUserXpResponse</returns>
public TrackablesBatchSetUserXpResponse TrackablesBatchSetUserXp(string token, TrackablesBatchSetUserXpRequest body)
{
	Snapser.Client.ApiResponse<TrackablesBatchSetUserXpResponse> localVarResponse = TrackablesBatchSetUserXpWithHttpInfo(token, body);
	return localVarResponse.Data;
}

/// Snapser.Model for TrackablesBatchSetUserXpRequest
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesBatchSetUserXpRequest" /> class.
/// </summary>
/// <param name="requests">requests.</param>
public TrackablesBatchSetUserXpRequest(List<TrackablesSetUserXpRequest> requests = default(List<TrackablesSetUserXpRequest>))
{
	this.Requests = requests;
}

/// Snapser.Model for TrackablesSetUserXpRequest
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesSetUserXpRequest" /> class.
/// </summary>
/// <param name="name">Name of xp to set (required).</param>
/// <param name="userId">UserID of the affected user (required).</param>
/// <param name="value">Value to be set as the user xp statistic (required).</param>
public TrackablesSetUserXpRequest(string name = default(string), string userId = default(string), long value = default(long))
{
	// to ensure "name" is required (not null)
	if (name == null)
	{
		throw new ArgumentNullException("name is a required property for TrackablesSetUserXpRequest and cannot be null");
	}
	this.Name = name;
	// to ensure "userId" is required (not null)
	if (userId == null)
	{
		throw new ArgumentNullException("userId is a required property for TrackablesSetUserXpRequest and cannot be null");
	}
	this.UserId = userId;
	this.Value = value;
}

/// Snapser.Model for TrackablesBatchSetUserXpResponse
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesBatchSetUserXpResponse" /> class.
/// </summary>
/// <param name="responses">responses.</param>
public TrackablesBatchSetUserXpResponse(List<TrackablesBatchSetSingleUserXpResponse> responses = default(List<TrackablesBatchSetSingleUserXpResponse>))
{
	this.Responses = responses;
}

/// Snapser.Model for TrackablesBatchSetSingleUserXpResponse
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesBatchSetSingleUserXpResponse" /> class.
/// </summary>
/// <param name="message">message.</param>
/// <param name="request">request.</param>
/// <param name="response">response.</param>
/// <param name="success">success.</param>
public TrackablesBatchSetSingleUserXpResponse(string message = default(string), TrackablesSetUserXpRequest request = default(TrackablesSetUserXpRequest), TrackablesUpdateUserXpInfo response = default(TrackablesUpdateUserXpInfo), bool success = default(bool))
{
	this.Message = message;
	this.Request = request;
	this.Response = response;
	this.Success = success;
}

/// Snapser.Model for TrackablesUpdateUserXpInfo
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesUpdateUserXpInfo" /> class.
/// </summary>
/// <param name="afterXp">Xp of user before the update.</param>
/// <param name="beforeXp">Xp of user before the update.</param>
/// <param name="currentLevel">currentLevel.</param>
/// <param name="levelCompletionRewards">Array of reward grant statuses for level completion rewards for each level progressed.</param>
/// <param name="levelUp">Boolean indicating if the user have levelled up because of update.</param>
/// <param name="levelUpRewards">Array of reward grant statuses for level up rewards for each level progressed.</param>
/// <param name="levelsProgressed">Array of levels progressed by the user in the event of level up.</param>
/// <param name="metadata">Metadata associated with the xp setting.</param>
/// <param name="nextLevels">Up to next 3 levels in progression.</param>
/// <param name="overflowXp">Remaining Xp after final level reached.</param>
/// <param name="rewardGrantStatuses">Array of reward grant statuses for each level progressed (DEPRECATED: Use level_completion_rewards, or level_up_rewards).</param>
/// <param name="rewardsGrantStatusPerLevel">Boolean indicator of rewards granted per level progressed (Deprecated: Use level_completion_rewards, or level_up_rewards).</param>
/// <param name="rewardsGranted">Boolean indicating if the user have been granted rewards successfully for all the levels progressed.</param>
/// <param name="success">Status of xp update, true if xp updated successfully.</param>
/// <param name="userId">UserID of the user.</param>
public TrackablesUpdateUserXpInfo(long afterXp = default(long), long beforeXp = default(long), TrackablesLevel currentLevel = default(TrackablesLevel), List<TrackablesRewardGrantStatus> levelCompletionRewards = default(List<TrackablesRewardGrantStatus>), bool levelUp = default(bool), List<TrackablesRewardGrantStatus> levelUpRewards = default(List<TrackablesRewardGrantStatus>), List<TrackablesLevel> levelsProgressed = default(List<TrackablesLevel>), Object metadata = default(Object), List<TrackablesLevel> nextLevels = default(List<TrackablesLevel>), long overflowXp = default(long), List<TrackablesRewardGrantStatus> rewardGrantStatuses = default(List<TrackablesRewardGrantStatus>), Dictionary<string, bool> rewardsGrantStatusPerLevel = default(Dictionary<string, bool>), bool rewardsGranted = default(bool), bool success = default(bool), string userId = default(string))
{
	this.AfterXp = afterXp;
	this.BeforeXp = beforeXp;
	this.CurrentLevel = currentLevel;
	this.LevelCompletionRewards = levelCompletionRewards;
	this.LevelUp = levelUp;
	this.LevelUpRewards = levelUpRewards;
	this.LevelsProgressed = levelsProgressed;
	this.Metadata = metadata;
	this.NextLevels = nextLevels;
	this.OverflowXp = overflowXp;
	this.RewardGrantStatuses = rewardGrantStatuses;
	this.RewardsGrantStatusPerLevel = rewardsGrantStatusPerLevel;
	this.RewardsGranted = rewardsGranted;
	this.Success = success;
	this.UserId = userId;
}

/// Snapser.Model for TrackablesRewardGrantStatus
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesRewardGrantStatus" /> class.
/// </summary>
/// <param name="dropTableResults">Results from drop table evaluations.</param>
/// <param name="failedDropTables">Drop tables that failed to grant.</param>
/// <param name="levelName">Name of the level for which the reward was granted.</param>
/// <param name="status">status.</param>
/// <param name="success">Indicates if the reward was granted successfully. Deprecated: Use status field.</param>
public TrackablesRewardGrantStatus(List<TrackablesDropTableGrantResult> dropTableResults = default, List<string> failedDropTables = default, string levelName = default, TrackablesRewardGrantStatusStatus? status = default, bool success = default)
{
	this.DropTableResults = dropTableResults;
	this.FailedDropTables = failedDropTables;
	this.LevelName = levelName;
	this.Status = status;
	this.Success = success;
}

/// Snapser.Model for TrackablesDropTableGrantResult
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesDropTableGrantResult" /> class.
/// </summary>
/// <param name="currenciesGranted">Currencies granted from this drop table evaluation.</param>
/// <param name="dropIndex">Index of the drop that was selected.</param>
/// <param name="dropTableName">Name of the drop table that was evaluated.</param>
/// <param name="energyGranted">Energy granted from this drop table evaluation.</param>
/// <param name="itemsGranted">Items granted from this drop table evaluation.</param>
public TrackablesDropTableGrantResult(Dictionary<string, string> currenciesGranted = default, int dropIndex = default, string dropTableName = default, Dictionary<string, int> energyGranted = default, Dictionary<string, int> itemsGranted = default)
{
	this.CurrenciesGranted = currenciesGranted;
	this.DropIndex = dropIndex;
	this.DropTableName = dropTableName;
	this.EnergyGranted = energyGranted;
	this.ItemsGranted = itemsGranted;
}

/// Snapser.Model for TrackablesLevel
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesLevel" /> class.
/// </summary>
/// <param name="end">Ending xp for the level.</param>
/// <param name="index">Index of the level in the xp setting.</param>
/// <param name="levelCompletionRewards">levelCompletionRewards.</param>
/// <param name="levelUpRewards">levelUpRewards.</param>
/// <param name="metadata">Metadata associated with the level.</param>
/// <param name="name">Name of the level.</param>
/// <param name="rewardCurrencies">Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardCurrencies64">64-bit Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardEnergies">Energies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardInventoryItems">Inventory items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardItems">Items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="start">Starting xp for the level.</param>
public TrackablesLevel(long end = default(long), int index = default(int), TrackablesRewards levelCompletionRewards = default(TrackablesRewards), TrackablesRewards levelUpRewards = default(TrackablesRewards), Object metadata = default(Object), string name = default(string), Dictionary<string, int> rewardCurrencies = default(Dictionary<string, int>), Dictionary<string, string> rewardCurrencies64 = default(Dictionary<string, string>), Dictionary<string, int> rewardEnergies = default(Dictionary<string, int>), Dictionary<string, TrackablesRewardInventoryItems> rewardInventoryItems = default(Dictionary<string, TrackablesRewardInventoryItems>), Dictionary<string, int> rewardItems = default(Dictionary<string, int>), long start = default(long))
{
	this.End = end;
	this.Index = index;
	this.LevelCompletionRewards = levelCompletionRewards;
	this.LevelUpRewards = levelUpRewards;
	this.Metadata = metadata;
	this.Name = name;
	this.RewardCurrencies = rewardCurrencies;
	this.RewardCurrencies64 = rewardCurrencies64;
	this.RewardEnergies = rewardEnergies;
	this.RewardInventoryItems = rewardInventoryItems;
	this.RewardItems = rewardItems;
	this.Start = start;
}

/// Snapser.Model for TrackablesRewards
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesRewards" /> class.
/// </summary>
/// <param name="currencies">Currencies to be rewarded.</param>
/// <param name="dropTables">Drop tables to grant.</param>
/// <param name="energies">Energies to be rewarded.</param>
/// <param name="items">Items to be rewarded.</param>
public TrackablesRewards(Dictionary<string, string> currencies = default, List<TrackablesRewardDropTable> dropTables = default, Dictionary<string, int> energies = default, Dictionary<string, TrackablesRewardInventoryItems> items = default)
{
	this.Currencies = currencies;
	this.DropTables = dropTables;
	this.Energies = energies;
	this.Items = items;
}

/// Snapser.Model for TrackablesRewardInventoryItems
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesRewardInventoryItems" /> class.
/// </summary>
/// <param name="count">Count of the inventory item to be rewarded.</param>
/// <param name="subInventoryItemName">Sub-inventory item name, if applicable.</param>
public TrackablesRewardInventoryItems(int count = default(int), string subInventoryItemName = default(string))
{
	this.Count = count;
	this.SubInventoryItemName = subInventoryItemName;
}

/// Snapser.Model for TrackablesRewardDropTable
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesRewardDropTable" /> class.
/// </summary>
/// <param name="count">Number of times to evaluate the drop table (default 1).</param>
/// <param name="name">Name of the drop table.</param>
public TrackablesRewardDropTable(int count = default, string name = default)
{
	this.Count = count;
	this.Name = name;
}
```
```cpp
// Request
class SNAPSER_API SnapserTrackablesServiceApi::TrackablesBatchSetUserXpRequest : public Request
{
public:
    virtual ~TrackablesBatchSetUserXpRequest() {}
	void SetupHttpRequest(const FHttpRequestRef& HttpRequest) const final;
	FString ComputePath() const final;

	/* (app auth only) */
	FString Token;
	SnapserTrackablesBatchSetUserXpRequest Body;
};

class SNAPSER_API SnapserTrackablesBatchSetUserXpRequest : public Model
{
public:
    virtual ~SnapserTrackablesBatchSetUserXpRequest() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	TOptional<TArray<SnapserTrackablesSetUserXpRequest>> Requests;
};

class SNAPSER_API SnapserTrackablesSetUserXpRequest : public Model
{
public:
    virtual ~SnapserTrackablesSetUserXpRequest() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Name of xp to set */
	FString Name;
	/* UserID of the affected user */
	FString UserId;
	/* Value to be set as the user xp statistic */
	int64 Value = 0;
};

// Response
class SNAPSER_API SnapserTrackablesServiceApi::TrackablesBatchSetUserXpResponse : public Response
{
public:
    virtual ~TrackablesBatchSetUserXpResponse() {}
	void SetHttpResponseCode(EHttpResponseCodes::Type InHttpResponseCode) final;
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;

    SnapserTrackablesBatchSetUserXpResponse Content;
};

class SNAPSER_API SnapserTrackablesBatchSetUserXpResponse : public Model
{
public:
    virtual ~SnapserTrackablesBatchSetUserXpResponse() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	TOptional<TArray<SnapserTrackablesBatchSetSingleUserXpResponse>> Responses;
};

class SNAPSER_API SnapserTrackablesBatchSetSingleUserXpResponse : public Model
{
public:
    virtual ~SnapserTrackablesBatchSetSingleUserXpResponse() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	TOptional<FString> Message;
	TOptional<SnapserTrackablesSetUserXpRequest> Request;
	TOptional<SnapserTrackablesUpdateUserXpInfo> Response;
	TOptional<bool> Success;
};

class SNAPSER_API SnapserTrackablesUpdateUserXpInfo : public Model
{
public:
    virtual ~SnapserTrackablesUpdateUserXpInfo() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Xp of user before the update */
	TOptional<int64> AfterXp;
	/* Xp of user before the update */
	TOptional<int64> BeforeXp;
	TOptional<SnapserTrackablesLevel> CurrentLevel;
	/* Array of reward grant statuses for level completion rewards for each level progressed */
	TOptional<TArray<SnapserTrackablesRewardGrantStatus>> LevelCompletionRewards;
	/* Boolean indicating if the user have levelled up because of update */
	TOptional<bool> LevelUp;
	/* Array of reward grant statuses for level up rewards for each level progressed */
	TOptional<TArray<SnapserTrackablesRewardGrantStatus>> LevelUpRewards;
	/* Array of levels progressed by the user in the event of level up */
	TOptional<TArray<SnapserTrackablesLevel>> LevelsProgressed;
	/* Metadata associated with the xp setting */
	TOptional<TSharedPtr<FJsonObject>> Metadata;
	/* Up to next 3 levels in progression */
	TOptional<TArray<SnapserTrackablesLevel>> NextLevels;
	/* Remaining Xp after final level reached */
	TOptional<int64> OverflowXp;
	/* Array of reward grant statuses for each level progressed (DEPRECATED: Use level_completion_rewards, or level_up_rewards) */
	TOptional<TArray<SnapserTrackablesRewardGrantStatus>> RewardGrantStatuses;
	/* Boolean indicator of rewards granted per level progressed (Deprecated: Use level_completion_rewards, or level_up_rewards) */
	TOptional<TMap<FString, bool>> RewardsGrantStatusPerLevel;
	/* Boolean indicating if the user have been granted rewards successfully for all the levels progressed */
	TOptional<bool> RewardsGranted;
	/* Status of xp update, true if xp updated successfully */
	TOptional<bool> Success;
	/* UserID of the user */
	TOptional<FString> UserId;
};

class SNAPSER_API SnapserTrackablesRewardGrantStatus : public Model
{
public:
    virtual ~SnapserTrackablesRewardGrantStatus() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Results from drop table evaluations */
	TOptional<TArray<SnapserTrackablesDropTableGrantResult>> DropTableResults;
	/* Drop tables that failed to grant */
	TOptional<TArray<FString>> FailedDropTables;
	/* Name of the level for which the reward was granted */
	TOptional<FString> LevelName;
	TOptional<SnapserTrackablesRewardGrantStatusStatus> Status;
	/* Indicates if the reward was granted successfully. Deprecated: Use status field */
	TOptional<bool> Success;
};

class SNAPSER_API SnapserTrackablesDropTableGrantResult : public Model
{
public:
    virtual ~SnapserTrackablesDropTableGrantResult() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Currencies granted from this drop table evaluation */
	TOptional<TMap<FString, FString>> CurrenciesGranted;
	/* Index of the drop that was selected */
	TOptional<int32> DropIndex;
	/* Name of the drop table that was evaluated */
	TOptional<FString> DropTableName;
	/* Energy granted from this drop table evaluation */
	TOptional<TMap<FString, int32>> EnergyGranted;
	/* Items granted from this drop table evaluation */
	TOptional<TMap<FString, int32>> ItemsGranted;
};

class SNAPSER_API SnapserTrackablesRewardGrantStatusStatus : public Model
{
public:
    virtual ~SnapserTrackablesRewardGrantStatusStatus() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	enum class Values
	{
		UnknownStatus,
		Granted,
		Failed,
		NotConfigured,
		Skipped,
  	};

	Values Value;

	static FString EnumToString(const Values& EnumValue);
	static bool EnumFromString(const FString& EnumAsString, Values& EnumValue);
};

class SNAPSER_API SnapserTrackablesLevel : public Model
{
public:
    virtual ~SnapserTrackablesLevel() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Ending xp for the level */
	TOptional<int64> End;
	/* Index of the level in the xp setting */
	TOptional<int32> Index;
	TOptional<SnapserTrackablesRewards> LevelCompletionRewards;
	TOptional<SnapserTrackablesRewards> LevelUpRewards;
	/* Metadata associated with the level */
	TOptional<TSharedPtr<FJsonObject>> Metadata;
	/* Name of the level */
	TOptional<FString> Name;
	/* Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, int32>> RewardCurrencies;
	/* 64-bit Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, FString>> RewardCurrencies64;
	/* Energies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, int32>> RewardEnergies;
	/* Inventory items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, SnapserTrackablesRewardInventoryItems>> RewardInventoryItems;
	/* Items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, int32>> RewardItems;
	/* Starting xp for the level */
	TOptional<int64> Start;
};

class SNAPSER_API SnapserTrackablesRewards : public Model
{
public:
    virtual ~SnapserTrackablesRewards() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Currencies to be rewarded */
	TOptional<TMap<FString, FString>> Currencies;
	/* Drop tables to grant */
	TOptional<TArray<SnapserTrackablesRewardDropTable>> DropTables;
	/* Energies to be rewarded */
	TOptional<TMap<FString, int32>> Energies;
	/* Items to be rewarded */
	TOptional<TMap<FString, SnapserTrackablesRewardInventoryItems>> Items;
};

class SNAPSER_API SnapserTrackablesRewardDropTable : public Model
{
public:
    virtual ~SnapserTrackablesRewardDropTable() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Number of times to evaluate the drop table (default 1) */
	TOptional<int32> Count;
	/* Name of the drop table */
	TOptional<FString> Name;
};

class SNAPSER_API SnapserTrackablesRewardInventoryItems : public Model
{
public:
    virtual ~SnapserTrackablesRewardInventoryItems() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Count of the inventory item to be rewarded */
	TOptional<int32> Count;
	/* Sub-inventory item name, if applicable */
	TOptional<FString> SubInventoryItemName;
};

```
```go
// Coming soon
```
```python
# Coming soon
```
</CodeGroup>

#### TrackablesGetXp API
<CodeGroup title="Gets the setting of an XP" tag="GET" label="TrackablesGetXp">
```csharp
/// <summary>
/// XP Setting Gets the setting of an XP
/// </summary>
/// <exception cref="Snapser.Client.ApiException">Thrown when fails to make API call</exception>
/// <param name="xpName">Name of XP to get</param>
/// <param name="userId">User ID of the user</param>
/// <returns>TrackablesGetXpResponse</returns>
public TrackablesGetXpResponse TrackablesGetXp(string xpName, string userId)
{
    Snapser.Client.ApiResponse<TrackablesGetXpResponse> localVarResponse = TrackablesGetXpWithHttpInfo(xpName, userId);
    return localVarResponse.Data;
}

/// Snapser.Model for TrackablesGetXpResponse
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesGetXpResponse" /> class.
/// </summary>
/// <param name="settings">settings.</param>
public TrackablesGetXpResponse(TrackablesXpSetting settings = default(TrackablesXpSetting))
{
    this.Settings = settings;
}

/// Snapser.Model for TrackablesXpSetting
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesXpSetting" /> class.
/// </summary>
/// <param name="autoResetOnComplete">Whether to auto reset xp on completion of final level.</param>
/// <param name="defaultXp">Default xp for new users.</param>
/// <param name="levels">List of levels in the xp setting (required).</param>
/// <param name="metadata">Metadata associated with the xp setting.</param>
/// <param name="name">Name of the xp setting (required).</param>
/// <param name="prerequisites">Prerequisites for the xp setting.</param>
/// <param name="scope">Scope of the xp setting (required).</param>
public TrackablesXpSetting(bool autoResetOnComplete = default(bool), long defaultXp = default(long), List<TrackablesLevel> levels = default(List<TrackablesLevel>), Object metadata = default(Object), string name = default(string), List<TrackablesPredicateGroup> prerequisites = default(List<TrackablesPredicateGroup>), string scope = default(string))
{
	this.Levels = levels;
	this.Scope = scope;
	this.AutoResetOnComplete = autoResetOnComplete;
	this.DefaultXp = defaultXp;
	this.Metadata = metadata;
	this.Prerequisites = prerequisites;
}

/// Snapser.Model for TrackablesLevel
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesLevel" /> class.
/// </summary>
/// <param name="end">Ending xp for the level.</param>
/// <param name="index">Index of the level in the xp setting.</param>
/// <param name="levelCompletionRewards">levelCompletionRewards.</param>
/// <param name="levelUpRewards">levelUpRewards.</param>
/// <param name="metadata">Metadata associated with the level.</param>
/// <param name="name">Name of the level.</param>
/// <param name="rewardCurrencies">Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardCurrencies64">64-bit Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardEnergies">Energies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardInventoryItems">Inventory items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardItems">Items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="start">Starting xp for the level.</param>
public TrackablesLevel(long end = default(long), int index = default(int), TrackablesRewards levelCompletionRewards = default(TrackablesRewards), TrackablesRewards levelUpRewards = default(TrackablesRewards), Object metadata = default(Object), string name = default(string), Dictionary<string, int> rewardCurrencies = default(Dictionary<string, int>), Dictionary<string, string> rewardCurrencies64 = default(Dictionary<string, string>), Dictionary<string, int> rewardEnergies = default(Dictionary<string, int>), Dictionary<string, TrackablesRewardInventoryItems> rewardInventoryItems = default(Dictionary<string, TrackablesRewardInventoryItems>), Dictionary<string, int> rewardItems = default(Dictionary<string, int>), long start = default(long))
{
	this.End = end;
	this.Index = index;
	this.LevelCompletionRewards = levelCompletionRewards;
	this.LevelUpRewards = levelUpRewards;
	this.Metadata = metadata;
	this.Name = name;
	this.RewardCurrencies = rewardCurrencies;
	this.RewardCurrencies64 = rewardCurrencies64;
	this.RewardEnergies = rewardEnergies;
	this.RewardInventoryItems = rewardInventoryItems;
	this.RewardItems = rewardItems;
	this.Start = start;
}

/// Snapser.Model for TrackablesRewards
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesRewards" /> class.
/// </summary>
/// <param name="currencies">Currencies to be rewarded.</param>
/// <param name="dropTables">Drop tables to grant.</param>
/// <param name="energies">Energies to be rewarded.</param>
/// <param name="items">Items to be rewarded.</param>
public TrackablesRewards(Dictionary<string, string> currencies = default, List<TrackablesRewardDropTable> dropTables = default, Dictionary<string, int> energies = default, Dictionary<string, TrackablesRewardInventoryItems> items = default)
{
	this.Currencies = currencies;
	this.DropTables = dropTables;
	this.Energies = energies;
	this.Items = items;
}

/// Snapser.Model for TrackablesRewardInventoryItems
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesRewardInventoryItems" /> class.
/// </summary>
/// <param name="count">Count of the inventory item to be rewarded.</param>
/// <param name="subInventoryItemName">Sub-inventory item name, if applicable.</param>
public TrackablesRewardInventoryItems(int count = default(int), string subInventoryItemName = default(string))
{
	this.Count = count;
	this.SubInventoryItemName = subInventoryItemName;
}

/// Snapser.Model for TrackablesRewardDropTable
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesRewardDropTable" /> class.
/// </summary>
/// <param name="count">Number of times to evaluate the drop table (default 1).</param>
/// <param name="name">Name of the drop table.</param>
public TrackablesRewardDropTable(int count = default, string name = default)
{
	this.Count = count;
	this.Name = name;
}

/// Snapser.Model for TrackablesPredicateGroup
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesPredicateGroup" /> class.
/// </summary>
/// <param name="predicates">predicates.</param>
public TrackablesPredicateGroup(List<TrackablesPredicate> predicates = default(List<TrackablesPredicate>))
{
    this.Predicates = predicates;
}

/// Snapser.Model for TrackablesPredicate
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesPredicate" /> class.
/// </summary>
/// <param name="category">category.</param>
/// <param name="name">name.</param>
/// <param name="varOperator">varOperator.</param>
/// <param name="value">value.</param>
public TrackablesPredicate(string category = default(string), string name = default(string), string varOperator = default(string), string value = default(string))
{
    this.Category = category;
    this.Name = name;
    this.VarOperator = varOperator;
    this.Value = value;
}
```
```cpp
// Request
class SNAPSER_API SnapserTrackablesServiceApi::TrackablesGetXpRequest : public Request
{
public:
    virtual ~TrackablesGetXpRequest() {}
    void SetupHttpRequest(const FHttpRequestRef& HttpRequest) const final;
    FString ComputePath() const final;

    /* Name of XP to get */
    FString XpName;
    /* User ID of the user */
    FString UserId;
};

// Response
class SNAPSER_API SnapserTrackablesServiceApi::TrackablesGetXpResponse : public Response
{
public:
    virtual ~TrackablesGetXpResponse() {}
    void SetHttpResponseCode(EHttpResponseCodes::Type InHttpResponseCode) final;
    bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;

    SnapserTrackablesGetXpResponse Content;
};

class SNAPSER_API SnapserTrackablesGetXpResponse : public Model
{
public:
    virtual ~SnapserTrackablesGetXpResponse() {}
    bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
    void WriteJson(JsonWriter& Writer) const final;

    TOptional<SnapserTrackablesXpSetting> Settings;
};

class SNAPSER_API SnapserTrackablesXpSetting : public Model
{
public:
    virtual ~SnapserTrackablesXpSetting() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Whether to auto reset xp on completion of final level */
	TOptional<bool> AutoResetOnComplete;
	/* Default xp for new users */
	TOptional<int64> DefaultXp;
	/* List of levels in the xp setting */
	TArray<SnapserTrackablesLevel> Levels;
	/* Metadata associated with the xp setting */
	TOptional<TSharedPtr<FJsonObject>> Metadata;
	/* Name of the xp setting */
	FString Name;
	/* Prerequisites for the xp setting */
	TOptional<TArray<SnapserTrackablesPredicateGroup>> Prerequisites;
	/* Scope of the xp setting */
	FString Scope;
};

class SNAPSER_API SnapserTrackablesLevel : public Model
{
public:
    virtual ~SnapserTrackablesLevel() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Ending xp for the level */
	TOptional<int64> End;
	/* Index of the level in the xp setting */
	TOptional<int32> Index;
	TOptional<SnapserTrackablesRewards> LevelCompletionRewards;
	TOptional<SnapserTrackablesRewards> LevelUpRewards;
	/* Metadata associated with the level */
	TOptional<TSharedPtr<FJsonObject>> Metadata;
	/* Name of the level */
	TOptional<FString> Name;
	/* Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, int32>> RewardCurrencies;
	/* 64-bit Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, FString>> RewardCurrencies64;
	/* Energies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, int32>> RewardEnergies;
	/* Inventory items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, SnapserTrackablesRewardInventoryItems>> RewardInventoryItems;
	/* Items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, int32>> RewardItems;
	/* Starting xp for the level */
	TOptional<int64> Start;
};

class SNAPSER_API SnapserTrackablesRewards : public Model
{
public:
    virtual ~SnapserTrackablesRewards() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Currencies to be rewarded */
	TOptional<TMap<FString, FString>> Currencies;
	/* Drop tables to grant */
	TOptional<TArray<SnapserTrackablesRewardDropTable>> DropTables;
	/* Energies to be rewarded */
	TOptional<TMap<FString, int32>> Energies;
	/* Items to be rewarded */
	TOptional<TMap<FString, SnapserTrackablesRewardInventoryItems>> Items;
};

class SNAPSER_API SnapserTrackablesRewardDropTable : public Model
{
public:
    virtual ~SnapserTrackablesRewardDropTable() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Number of times to evaluate the drop table (default 1) */
	TOptional<int32> Count;
	/* Name of the drop table */
	TOptional<FString> Name;
};

class SNAPSER_API SnapserTrackablesRewardInventoryItems : public Model
{
public:
    virtual ~SnapserTrackablesRewardInventoryItems() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Count of the inventory item to be rewarded */
	TOptional<int32> Count;
	/* Sub-inventory item name, if applicable */
	TOptional<FString> SubInventoryItemName;
};

class SNAPSER_API SnapserTrackablesPredicateGroup : public Model
{
public:
    virtual ~SnapserTrackablesPredicateGroup() {}
    bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
    void WriteJson(JsonWriter& Writer) const final;

    TOptional<TArray<SnapserTrackablesPredicate>> Predicates;
};

class SNAPSER_API SnapserTrackablesPredicate : public Model
{
public:
    virtual ~SnapserTrackablesPredicate() {}
    bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
    void WriteJson(JsonWriter& Writer) const final;

    TOptional<FString> Category;
    TOptional<FString> Name;
    TOptional<FString> _Operator;
    TOptional<FString> Value;
};

```
```go
// Coming soon
```
```python
# Coming soon
```
</CodeGroup>

#### TrackablesGetXps API
<CodeGroup title="Gets the settings of multiple XPs" tag="GET" label="TrackablesGetXps">
```csharp
/// <summary>
/// XP Settings Gets the settings of multiple XPs
/// </summary>
/// <exception cref="Snapser.Client.ApiException">Thrown when fails to make API call</exception>
/// <param name="names">Names of XPs to get</param>
/// <param name="userId">User ID of the user</param>
/// <returns>TrackablesGetXpsResponse</returns>
public TrackablesGetXpsResponse TrackablesGetXps(List<string> names, string userId)
{
    Snapser.Client.ApiResponse<TrackablesGetXpsResponse> localVarResponse = TrackablesGetXpsWithHttpInfo(names, userId);
    return localVarResponse.Data;
}

/// Snapser.Model for TrackablesGetXpsResponse
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesGetXpsResponse" /> class.
/// </summary>
/// <param name="responses">responses.</param>
/// <param name="settings">settings.</param>
public TrackablesGetXpsResponse(List<TrackablesBatchGetXpSingleResponse> responses = default(List<TrackablesBatchGetXpSingleResponse>), List<TrackablesXpSetting> settings = default(List<TrackablesXpSetting>))
{
    this.Responses = responses;
    this.Settings = settings;
}

/// Snapser.Model for TrackablesBatchGetXpSingleResponse
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesBatchGetXpSingleResponse" /> class.
/// </summary>
/// <param name="message">message.</param>
/// <param name="request">request.</param>
/// <param name="response">response.</param>
/// <param name="success">success.</param>
public TrackablesBatchGetXpSingleResponse(string message = default(string), TrackablesGetXpRequest request = default(TrackablesGetXpRequest), TrackablesXpSetting response = default(TrackablesXpSetting), bool success = default(bool))
{
    this.Message = message;
    this.Request = request;
    this.Response = response;
    this.Success = success;
}

/// Snapser.Model for TrackablesGetXpRequest
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesGetXpRequest" /> class.
/// </summary>
/// <param name="xpName">Name of XP to get (required).</param>
public TrackablesGetXpRequest(string xpName = default(string))
{
    // to ensure "xpName" is required (not null)
    if (xpName == null)
    {
        throw new ArgumentNullException("xpName is a required property for TrackablesGetXpRequest and cannot be null");
    }
    this.XpName = xpName;
}

/// Snapser.Model for TrackablesXpSetting
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesXpSetting" /> class.
/// </summary>
/// <param name="autoResetOnComplete">Whether to auto reset xp on completion of final level.</param>
/// <param name="defaultXp">Default xp for new users.</param>
/// <param name="levels">List of levels in the xp setting (required).</param>
/// <param name="metadata">Metadata associated with the xp setting.</param>
/// <param name="name">Name of the xp setting (required).</param>
/// <param name="prerequisites">Prerequisites for the xp setting.</param>
/// <param name="scope">Scope of the xp setting (required).</param>
public TrackablesXpSetting(bool autoResetOnComplete = default(bool), long defaultXp = default(long), List<TrackablesLevel> levels = default(List<TrackablesLevel>), Object metadata = default(Object), string name = default(string), List<TrackablesPredicateGroup> prerequisites = default(List<TrackablesPredicateGroup>), string scope = default(string))
{
	this.Levels = levels;
	this.Scope = scope;
	this.AutoResetOnComplete = autoResetOnComplete;
	this.DefaultXp = defaultXp;
	this.Metadata = metadata;
	this.Prerequisites = prerequisites;
}

/// Snapser.Model for TrackablesLevel
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesLevel" /> class.
/// </summary>
/// <param name="end">Ending xp for the level.</param>
/// <param name="index">Index of the level in the xp setting.</param>
/// <param name="levelCompletionRewards">levelCompletionRewards.</param>
/// <param name="levelUpRewards">levelUpRewards.</param>
/// <param name="metadata">Metadata associated with the level.</param>
/// <param name="name">Name of the level.</param>
/// <param name="rewardCurrencies">Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardCurrencies64">64-bit Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardEnergies">Energies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardInventoryItems">Inventory items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="rewardItems">Items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead).</param>
/// <param name="start">Starting xp for the level.</param>
public TrackablesLevel(long end = default(long), int index = default(int), TrackablesRewards levelCompletionRewards = default(TrackablesRewards), TrackablesRewards levelUpRewards = default(TrackablesRewards), Object metadata = default(Object), string name = default(string), Dictionary<string, int> rewardCurrencies = default(Dictionary<string, int>), Dictionary<string, string> rewardCurrencies64 = default(Dictionary<string, string>), Dictionary<string, int> rewardEnergies = default(Dictionary<string, int>), Dictionary<string, TrackablesRewardInventoryItems> rewardInventoryItems = default(Dictionary<string, TrackablesRewardInventoryItems>), Dictionary<string, int> rewardItems = default(Dictionary<string, int>), long start = default(long))
{
	this.End = end;
	this.Index = index;
	this.LevelCompletionRewards = levelCompletionRewards;
	this.LevelUpRewards = levelUpRewards;
	this.Metadata = metadata;
	this.Name = name;
	this.RewardCurrencies = rewardCurrencies;
	this.RewardCurrencies64 = rewardCurrencies64;
	this.RewardEnergies = rewardEnergies;
	this.RewardInventoryItems = rewardInventoryItems;
	this.RewardItems = rewardItems;
	this.Start = start;
}

/// Snapser.Model for TrackablesRewards
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesRewards" /> class.
/// </summary>
/// <param name="currencies">Currencies to be rewarded.</param>
/// <param name="dropTables">Drop tables to grant.</param>
/// <param name="energies">Energies to be rewarded.</param>
/// <param name="items">Items to be rewarded.</param>
public TrackablesRewards(Dictionary<string, string> currencies = default, List<TrackablesRewardDropTable> dropTables = default, Dictionary<string, int> energies = default, Dictionary<string, TrackablesRewardInventoryItems> items = default)
{
	this.Currencies = currencies;
	this.DropTables = dropTables;
	this.Energies = energies;
	this.Items = items;
}

/// Snapser.Model for TrackablesRewardInventoryItems
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesRewardInventoryItems" /> class.
/// </summary>
/// <param name="count">Count of the inventory item to be rewarded.</param>
/// <param name="subInventoryItemName">Sub-inventory item name, if applicable.</param>
public TrackablesRewardInventoryItems(int count = default(int), string subInventoryItemName = default(string))
{
	this.Count = count;
	this.SubInventoryItemName = subInventoryItemName;
}

/// Snapser.Model for TrackablesRewardDropTable
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesRewardDropTable" /> class.
/// </summary>
/// <param name="count">Number of times to evaluate the drop table (default 1).</param>
/// <param name="name">Name of the drop table.</param>
public TrackablesRewardDropTable(int count = default, string name = default)
{
	this.Count = count;
	this.Name = name;
}

/// Snapser.Model for TrackablesPredicateGroup
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesPredicateGroup" /> class.
/// </summary>
/// <param name="predicates">predicates.</param>
public TrackablesPredicateGroup(List<TrackablesPredicate> predicates = default(List<TrackablesPredicate>))
{
    this.Predicates = predicates;
}

/// Snapser.Model for TrackablesPredicate
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesPredicate" /> class.
/// </summary>
/// <param name="category">category.</param>
/// <param name="name">name.</param>
/// <param name="varOperator">varOperator.</param>
/// <param name="value">value.</param>
public TrackablesPredicate(string category = default(string), string name = default(string), string varOperator = default(string), string value = default(string))
{
    this.Category = category;
    this.Name = name;
    this.VarOperator = varOperator;
    this.Value = value;
}
```
```cpp
// Request
class SNAPSER_API SnapserTrackablesServiceApi::TrackablesGetXpsRequest : public Request
{
public:
    virtual ~TrackablesGetXpsRequest() {}
    void SetupHttpRequest(const FHttpRequestRef& HttpRequest) const final;
    FString ComputePath() const final;

    /* Names of XPs to get */
    TArray<FString> Names;
    /* User ID of the user */
    FString UserId;
};

// Response
class SNAPSER_API SnapserTrackablesServiceApi::TrackablesGetXpsResponse : public Response
{
public:
    virtual ~TrackablesGetXpsResponse() {}
    void SetHttpResponseCode(EHttpResponseCodes::Type InHttpResponseCode) final;
    bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;

    SnapserTrackablesGetXpsResponse Content;
};

class SNAPSER_API SnapserTrackablesGetXpsResponse : public Model
{
public:
    virtual ~SnapserTrackablesGetXpsResponse() {}
    bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
    void WriteJson(JsonWriter& Writer) const final;

    TOptional<TArray<SnapserTrackablesBatchGetXpSingleResponse>> Responses;
    TOptional<TArray<SnapserTrackablesXpSetting>> Settings;
};

class SNAPSER_API SnapserTrackablesBatchGetXpSingleResponse : public Model
{
public:
    virtual ~SnapserTrackablesBatchGetXpSingleResponse() {}
    bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
    void WriteJson(JsonWriter& Writer) const final;

    TOptional<FString> Message;
    TOptional<SnapserTrackablesGetXpRequest> Request;
    TOptional<SnapserTrackablesXpSetting> Response;
    TOptional<bool> Success;
};

class SNAPSER_API SnapserTrackablesGetXpRequest : public Model
{
public:
    virtual ~SnapserTrackablesGetXpRequest() {}
    bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
    void WriteJson(JsonWriter& Writer) const final;

    /* Name of XP to get */
    FString XpName;
};

class SNAPSER_API SnapserTrackablesXpSetting : public Model
{
public:
    virtual ~SnapserTrackablesXpSetting() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Whether to auto reset xp on completion of final level */
	TOptional<bool> AutoResetOnComplete;
	/* Default xp for new users */
	TOptional<int64> DefaultXp;
	/* List of levels in the xp setting */
	TArray<SnapserTrackablesLevel> Levels;
	/* Metadata associated with the xp setting */
	TOptional<TSharedPtr<FJsonObject>> Metadata;
	/* Name of the xp setting */
	FString Name;
	/* Prerequisites for the xp setting */
	TOptional<TArray<SnapserTrackablesPredicateGroup>> Prerequisites;
	/* Scope of the xp setting */
	FString Scope;
};

class SNAPSER_API SnapserTrackablesLevel : public Model
{
public:
    virtual ~SnapserTrackablesLevel() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Ending xp for the level */
	TOptional<int64> End;
	/* Index of the level in the xp setting */
	TOptional<int32> Index;
	TOptional<SnapserTrackablesRewards> LevelCompletionRewards;
	TOptional<SnapserTrackablesRewards> LevelUpRewards;
	/* Metadata associated with the level */
	TOptional<TSharedPtr<FJsonObject>> Metadata;
	/* Name of the level */
	TOptional<FString> Name;
	/* Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, int32>> RewardCurrencies;
	/* 64-bit Currencies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, FString>> RewardCurrencies64;
	/* Energies rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, int32>> RewardEnergies;
	/* Inventory items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, SnapserTrackablesRewardInventoryItems>> RewardInventoryItems;
	/* Items rewarded upon leveling up to this level (DEPRECATED: Use level_up_rewards and level_completion_rewards instead) */
	TOptional<TMap<FString, int32>> RewardItems;
	/* Starting xp for the level */
	TOptional<int64> Start;
};

class SNAPSER_API SnapserTrackablesRewards : public Model
{
public:
    virtual ~SnapserTrackablesRewards() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Currencies to be rewarded */
	TOptional<TMap<FString, FString>> Currencies;
	/* Drop tables to grant */
	TOptional<TArray<SnapserTrackablesRewardDropTable>> DropTables;
	/* Energies to be rewarded */
	TOptional<TMap<FString, int32>> Energies;
	/* Items to be rewarded */
	TOptional<TMap<FString, SnapserTrackablesRewardInventoryItems>> Items;
};

class SNAPSER_API SnapserTrackablesRewardDropTable : public Model
{
public:
    virtual ~SnapserTrackablesRewardDropTable() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Number of times to evaluate the drop table (default 1) */
	TOptional<int32> Count;
	/* Name of the drop table */
	TOptional<FString> Name;
};

class SNAPSER_API SnapserTrackablesRewardInventoryItems : public Model
{
public:
    virtual ~SnapserTrackablesRewardInventoryItems() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Count of the inventory item to be rewarded */
	TOptional<int32> Count;
	/* Sub-inventory item name, if applicable */
	TOptional<FString> SubInventoryItemName;
};

class SNAPSER_API SnapserTrackablesPredicateGroup : public Model
{
public:
    virtual ~SnapserTrackablesPredicateGroup() {}
    bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
    void WriteJson(JsonWriter& Writer) const final;

    TOptional<TArray<SnapserTrackablesPredicate>> Predicates;
};

class SNAPSER_API SnapserTrackablesPredicate : public Model
{
public:
    virtual ~SnapserTrackablesPredicate() {}
    bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
    void WriteJson(JsonWriter& Writer) const final;

    TOptional<FString> Category;
    TOptional<FString> Name;
    TOptional<FString> _Operator;
    TOptional<FString> Value;
};
```
```go
// Coming soon
```
```python
# Coming soon
```
</CodeGroup>

#### TrackablesGetEnergy API
<CodeGroup title="Gets the settings of multiple Energy Trackables" tag="GET" label="TrackablesGetEnergy">
```csharp
/// <summary>
/// Energy Settings Gets the settings of multiple Energy Trackables
/// </summary>
/// <exception cref="Snapser.Client.ApiException">Thrown when fails to make API call</exception>
/// <param name="name">Names of Energy trackables to get</param>
/// <param name="userId">User ID of the user</param>
/// <returns>TrackablesGetEnergyResponse</returns>
public TrackablesGetEnergyResponse TrackablesGetEnergy(List<string> name, string userId)
{
    Snapser.Client.ApiResponse<TrackablesGetEnergyResponse> localVarResponse = TrackablesGetEnergyWithHttpInfo(name, userId);
    return localVarResponse.Data;
}

/// Snapser.Model for TrackablesGetEnergyResponse
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesGetEnergyResponse" /> class.
/// </summary>
/// <param name="settings">settings of energy.</param>
public TrackablesGetEnergyResponse(List<TrackablesEnergySetting> settings = default(List<TrackablesEnergySetting>))
{
    this.Settings = settings;
}

/// Snapser.Model for TrackablesEnergySetting
/// <summary>
/// Initializes a new instance of the <see cref="TrackablesEnergySetting" /> class.
/// </summary>
/// <param name="defaultEnergy">Default energy for new users.</param>
/// <param name="maxEnergy">Maximum energy for the setting.</param>
/// <param name="metadata">Metadata associated with the energy setting.</param>
/// <param name="name">Name of the energy setting.</param>
/// <param name="overflow">Whether energy can overflow beyond max energy.</param>
/// <param name="scope">Scope of the energy setting.</param>
/// <param name="secondsToRecover">Seconds to recover one energy.</param>
public TrackablesEnergySetting(int defaultEnergy = default(int), int maxEnergy = default(int), Object metadata = default(Object), string name = default(string), bool overflow = default(bool), string scope = default(string), int secondsToRecover = default(int))
{
	this.DefaultEnergy = defaultEnergy;
	this.MaxEnergy = maxEnergy;
	this.Metadata = metadata;
	this.Name = name;
	this.Overflow = overflow;
	this.Scope = scope;
	this.SecondsToRecover = secondsToRecover;
}
```
```cpp
// Request
class SNAPSER_API SnapserTrackablesServiceApi::TrackablesGetEnergyRequest : public Request
{
public:
    virtual ~TrackablesGetEnergyRequest() {}
    void SetupHttpRequest(const FHttpRequestRef& HttpRequest) const final;
    FString ComputePath() const final;

    /* Names of Energy trackables to get */
    TArray<FString> Name;
    /* User ID of the user */
    FString UserId;
};

// Response
class SNAPSER_API SnapserTrackablesServiceApi::TrackablesGetEnergyResponse : public Response
{
public:
    virtual ~TrackablesGetEnergyResponse() {}
    void SetHttpResponseCode(EHttpResponseCodes::Type InHttpResponseCode) final;
    bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;

    SnapserTrackablesGetEnergyResponse Content;
};

class SNAPSER_API SnapserTrackablesGetEnergyResponse : public Model
{
public:
    virtual ~SnapserTrackablesGetEnergyResponse() {}
    bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
    void WriteJson(JsonWriter& Writer) const final;

    /* settings of energy */
    TOptional<TArray<SnapserTrackablesEnergySetting>> Settings;
};

class SNAPSER_API SnapserTrackablesEnergySetting : public Model
{
public:
    virtual ~SnapserTrackablesEnergySetting() {}
	bool FromJson(const TSharedPtr<FJsonValue>& JsonValue) final;
	void WriteJson(JsonWriter& Writer) const final;

	/* Default energy for new users */
	TOptional<int32> DefaultEnergy;
	/* Maximum energy for the setting */
	TOptional<int32> MaxEnergy;
	/* Metadata associated with the energy setting */
	TOptional<TSharedPtr<FJsonObject>> Metadata;
	/* Name of the energy setting */
	TOptional<FString> Name;
	/* Whether energy can overflow beyond max energy */
	TOptional<bool> Overflow;
	/* Scope of the energy setting */
	TOptional<FString> Scope;
	/* Seconds to recover one energy */
	TOptional<int32> SecondsToRecover;
};

```
```go
// Coming soon
```
```python
# Coming soon
```
</CodeGroup>

### API Explorer
You can view all the APIs exposed by this Snap via the API Explorer. On the Snapend home page,
in the left nav bar, click on `Tools > API Explorer` and then select the snap name to view the APIs.

![Api Pic](/images/docs/trackables-api.png)

![Api Pic](/images/docs/energy-api.png)


### Error Codes

Apart from these [generic errors](../generic-errors), below are snap specific error codes

<div className="errorsTable">
| Code  | Variable                      | Message                           | HTTP | gRPC             |
| ----- | ----------------------------- | --------------------------------- | ---- | ---------------- |
| 18000 | ErrXpSettingsAlreadyExists    | Xp setting already exists         | 400  | AlreadyExists    |
| 18001 | ErrXpSettingNotFound          | Xp setting not found              | 404  | NotFound         |
| 18002 | ErrUserXpNotSet               | User xp not set                   | 404  | NotFound         |
| 18003 | ErrInsufficientEnergy         | Insufficient energy               | 400  | InvalidArgument  |
| 18004 | ErrEnergySettingAlreadyExists | Energy setting already exists     | 400  | AlreadyExists    |
| 18005 | ErrEnergySettingNotFound      | Energy setting not found          | 404  | NotFound         |
| 18006 | ErrIncrementEnergyUnsupported | Incrementing energy not supported | 501  | Unimplemented    |
| 18007 | ErrXpOutOfRange               | Xp out of range                   | 400  | InvalidArgument  |
| 18008 | ErrRequirementsNotMet         | Requirements not met              | 403  | PermissionDenied |
| 18009 | ErrUserEnergyNotSet           | User energy not set               | 404  | NotFound         |
| 18010 | ErrInvalidXpValue             | Invalid xp value                  | 400  | InvalidArgument  |
| 18011 | ErrInvalidEnergyValue         | Invalid energy value              | 400  | InvalidArgument  |
| 18012 | ErrInvalidEnergyLossTsValue   | Invalid energy loss ts value      | 400  | InvalidArgument  |
</div>

### Websocket Emitters

When [Web sockets](/docs/infra/snaps/hermes) are enabled on your snapends gateway, and you
have added the [Event Bus](/docs/infra/snaps/eventbus) snap, this snap can broadcast specific
events related to statistic and segments changes directly to the game clients. These events are
targeted to the appropriate user. Each event is uniquely identified by an enum, and the event
payload is a well-structured protobuf message. These protobuf messages can be downloaded from
the Downloads page.

You can choose to enable/disable events from the [Event Bus](/docs/infra/snaps/eventbus) configuration tool.

| Event Id | Event Name                         | Description                                            | Payload Proto Message                |
| -------- | ---------------------------------- | ------------------------------------------------------ | ------------------------------------ |
| 1        | TRACKABLES_XP_PLAYER_LEVELED_UP    | Published on user XP level up                          | EventTrackablesXpPlayerLeveledUp     |


## Additional Details

### Data Stores

Postgres

### Service Dependencies

The trackables service depends on Statistics snap.

### Recommended Services

The trackables service works best when paired with the Auth service.

### Upcoming enhancements

We will very soon release a newer version of xp that will have ability to
award items to user based on their progression.
